# 模块改造计划

## 1. 改造目标

参考 `E:\Myproject\haorong-mall-master` 的 `haorong-upms` 与 `haorong-visual/haorong-generator` 设计，对本项目管理端核心模块做一次结构化对齐。

本次对齐不是照搬参考项目的微服务拆分，而是在本项目“模块化单体 + 前后端分离 + Sa-Token + MyBatis/Flyway”的现有架构内，吸收参考项目以下设计：

- 菜单、按钮、权限用一棵统一资源树表达。
- 角色授权围绕菜单树和按钮权限展开。
- 用户、部门、角色都以租户为作用域。
- 字典采用“字典类型 + 字典值”二级模型。
- 租户具备套餐、授权期限、启停状态、基础资料和可用菜单范围。
- 租户套餐可以表达应用/能力集合，并影响租户可见菜单。
- 代码生成具备数据源、数据表、字段元数据、模板配置、预览与下载能力。

## 2. 参考项目设计摘取

### 2.1 后端模块边界

参考项目集中在两个模块：

| 参考模块 | 覆盖内容 | 主要入口 |
|---|---|---|
| `haorong-upms` | 菜单、字典、租户、用户、角色、部门、租户套餐、租户菜单授权 | `/menu`、`/dict`、`/dictvalue`、`/tenant`、`/tenantpackage`、`/tenant-menu`、`/sysrole`、`/user`、`/sysdept` |
| `haorong-visual/haorong-generator` | 代码生成、数据源、数据表、字段元数据、模板 | `/gen-table`、`/gen-datasource` |

### 2.2 核心数据关系

参考项目的管理域主链路如下：

```text
租户套餐 sys_tenant_package
  └─ app_key: app_base, app_market, app_wechat ...

租户 sys_tenant
  ├─ package_id
  ├─ auth_begin_time / auth_end_time
  └─ status

租户菜单 sys_tenant_menu
  ├─ tenant_id
  └─ menu_id

菜单 sys_menu
  ├─ parent_id
  ├─ type: 0 菜单 / 1 按钮
  ├─ permission
  ├─ path / component / redirect / icon
  └─ application_key

角色 sys_role
  └─ sys_role_menu
      └─ menu_id

用户 sys_user
  ├─ dept_id
  └─ sys_user_role
      └─ role_id

部门 sys_dept
  └─ parent_id

字典 sys_dict
  └─ sys_dict_value
      ├─ dict_label
      ├─ dict_value
      ├─ dict_type
      ├─ sort
      └─ show_class
```

代码生成主链路如下：

```text
gen_data_source
  └─ gen_table
      └─ gen_table_column
          └─ vm 模板输出 Java / XML / TS / Vue
```

## 3. 本项目现状

本项目已经具备对应模块，但设计强弱不一致。

| 模块 | 当前位置 | 当前状态 | 对齐判断 |
|---|---|---|---|
| 菜单管理 | `modules/menu`、`frontend/src/views/platform/MenuManagementView.vue` | 已有统一 `sys_menu + sys_role_menu` 主链路 | 基础接近参考项目，需补齐展示字段、租户可用范围和按钮粒度体验 |
| 字典管理 | `modules/system`、`SystemDictsView.vue` | 已有 `sys_dict_value`，但 `sys_dict` 仍保留旧“字典项”痕迹 | 需要收敛为标准二级模型 |
| 租户管理 | `modules/tenant`、`TenantsView.vue` | 已有租户生命周期、变更历史、能力覆盖 | 比参考项目更复杂，但缺少“菜单范围/授权期限/租户基础资料”直观模型 |
| 用户管理 | `modules/user`、`UsersView.vue` | 已有用户、部门、角色关联 | 需补齐参考项目的角色必选、密码重置/修改、部门树联动和管理员保护体验 |
| 角色管理 | `modules/role`、`RolesView.vue` | 已有角色 CRUD 和菜单授权 | 需补齐分页、角色编码/名称唯一性提示、授权树半选状态和数据范围呈现 |
| 部门管理 | `modules/dept`、`DepartmentsView.vue` | 已有部门树 CRUD | 需补齐负责人、电话、排序、状态等运营字段 |
| 租户套餐 | `modules/tenant`、`TenantCatalogView.vue` | 当前是“套餐 + 能力 + 能力资源范围” | 设计强于参考项目，但需要提供参考项目式套餐展示和应用集合映射 |
| 代码生成 | `modules/codegen`、`CodegenView.vue` | 已有表白名单、预览、生成、下载、自定义模板 | 缺少参考项目的数据源管理、导入元数据、字段配置持久化 |

## 4. 总体改造原则

1. 保留本项目模块边界：`interfaces -> application -> domain / infrastructure`，不把参考项目的大平层 Controller/Service 直接搬进来。
2. 数据库通过新增 Flyway 脚本演进，不修改已执行历史脚本。
3. 菜单与权限继续以 `sys_menu + sys_role_menu` 为唯一主链路，不恢复旧 `sys_permission/sys_role_permission` 运行链路。
4. 租户套餐继续使用“套餐 + 能力 + 能力资源范围”的增强模型，但前台交互向参考项目的套餐表单看齐。
5. 前端页面统一采用“查询区 + 表格/树 + 右侧/弹窗表单 + 授权弹窗”的管理后台形态。
6. 所有模块都必须有租户作用域、软删除、审计字段和权限码登记。

## 5. 目标模块关系

```text
平台菜单模板 sys_menu(tenant_id = platform)
  ├─ DIR / MENU / BUTTON / API
  ├─ route_key / grant_key / path / component / icon
  └─ application_key

租户套餐 sys_tenant_package
  ├─ package_code / package_name
  ├─ sales_price / original_price / description
  └─ package_capability / app_key 映射

能力 sys_tenant_capability
  └─ capability_resource_scope
      ├─ VISIBLE: 控制租户菜单可见
      └─ GRANT: 控制角色可授权范围

租户 sys_tenant
  ├─ tenant_name / logo_url / address / site_url
  ├─ auth_begin_time / expire_at
  ├─ tenant_status
  └─ package_code

角色 sys_role
  └─ sys_role_menu

用户 sys_user
  ├─ dept_id
  └─ sys_user_role

部门 sys_dept
  └─ parent_id

字典 sys_dict
  └─ sys_dict_value

代码生成
  ├─ codegen_data_source
  ├─ codegen_table
  ├─ codegen_table_column
  ├─ codegen_template
  └─ sys_codegen_allowlist
```

## 6. 分模块改造计划

### 6.1 菜单管理

#### 当前保留

- 保留 `sys_menu` 的 `DIR/MENU/BUTTON/API` 四类节点。
- 保留 `resource_key`、`route_key`、`grant_key`，继续作为运行时权限快照来源。
- 保留 `template-tree`、`grantable-tree`、角色授权链路。

#### 参考项目对齐

参考项目 `sys_menu` 有 `name`、`permission`、`path`、`redirect`、`parent_id`、`icon`、`component`、`sort`、`type`、`outer_status`、`application_key`。

本项目需要在页面和接口表达上对齐这些概念：

| 参考字段 | 本项目字段 | 改造动作 |
|---|---|---|
| `name` | `menu_name` | 页面统一显示“菜单名称” |
| `permission` | `grant_key` | 表单显示为“权限标识” |
| `path` | `path` | 保留 |
| `redirect` | `redirect` | 页面补充字段 |
| `icon` | `icon` | 图标选择器补齐 |
| `component` | `component` | 页面补充组件路径/组件名说明 |
| `sort` | `order_no` | 页面统一显示“排序” |
| `type` | `menu_type` | 页面展示目录/菜单/按钮/API |
| `outer_status` | `outer_status` | 页面补充外链开关 |
| `application_key` | `application_key` | 用于套餐/能力映射 |

#### 改造项

- 菜单树表格增加：类型、权限标识、路由标识、组件、外链、应用标识、启用状态。
- 菜单表单按节点类型动态显示字段：
  - 目录：名称、父级、图标、排序、可见、应用标识。
  - 菜单：名称、父级、路由、组件、重定向、图标、排序、应用标识。
  - 按钮/API：名称、父级、权限标识、排序、应用标识。
- 删除菜单前做引用检查：角色授权、租户能力范围、子节点。
- 新增“复制权限标识”和“批量生成按钮权限”入口，向参考项目按钮权限管理体验看齐。
- 租户授权树优先从 `capability_resource_scope` 过滤，不直接引入 `sys_tenant_menu`，除非后续决定改为显式租户菜单表。

### 6.2 字典管理

#### 当前问题

本项目已有 `sys_dict_value`，但 `sys_dict` 当前还包含 `dict_code`、`dict_value`，容易和二级字典值混淆。

#### 目标结构

```text
sys_dict
  ├─ dict_type
  ├─ description
  ├─ status/enabled
  └─ remarks

sys_dict_value
  ├─ dict_id
  ├─ dict_type
  ├─ dict_label
  ├─ dict_value
  ├─ sort
  ├─ show_class
  ├─ enabled
  └─ remarks
```

#### 改造项

- 后端保留现有 API 兼容期，但内部语义收敛为：`sys_dict` 只表示字典类型，`sys_dict_value` 表示字典项。
- 新增字典详情接口：一次返回字典类型和字典值列表。
- 字典列表增加启停、备注、更新时间、字典值数量。
- 字典值管理从“按类型查询”升级为“字典详情内管理”。
- 字典值支持排序、回显样式、启停。
- 前端改为左右结构：左侧字典类型，右侧字典值表格。
- 缓存刷新保留，但增加刷新成功后的重新加载。

### 6.3 租户管理

#### 当前问题

本项目已有生命周期和能力覆盖，但参考项目的租户资料更贴近后台管理直觉：Logo、地址、官网、手机号、邮箱、授权起止时间、套餐。

#### 目标结构

在现有 `sys_tenant` 上补齐运营字段：

| 字段 | 说明 |
|---|---|
| `logo_url` / `logo_file_key` | 租户 Logo |
| `address` | 地址 |
| `site_url` | 官网 |
| `contact_phone` | 联系电话 |
| `contact_email` | 邮箱 |
| `auth_begin_time` | 授权开始时间 |
| `expire_at` | 授权结束时间，复用现有字段 |
| `package_code` | 套餐编码，保留现有字段 |
| `tenant_status` | 启停状态 |

#### 改造项

- 租户列表查询区增加：租户名称/编码、状态、套餐、到期状态。
- 租户表格增加：套餐、授权期限、状态、联系人、最近变更。
- 租户表单补齐：Logo、地址、官网、手机号、邮箱、授权开始/结束时间、套餐。
- 创建租户时自动初始化：默认部门、管理员用户、管理员角色、角色菜单授权。
- 保留当前变更历史能力，并把套餐变更、授权期限变更、状态变更纳入摘要。
- 能力覆盖页改造为“套餐默认能力 + 租户覆盖能力 + 实际菜单范围”三段式展示。

### 6.4 用户管理

#### 参考项目特征

- 用户关联部门和角色。
- 新增用户时角色不能为空。
- 修改用户时不直接修改密码和手机号。
- 删除管理员有保护。
- 提供密码修改入口。

#### 改造项

- 用户列表增加部门树过滤。
- 用户表格增加：部门、角色、手机号、邮箱、启停、最近登录、必须改密。
- 用户表单增加角色必选校验、部门选择、初始密码策略。
- 新增“重置密码”和“强制改密”操作。
- 删除用户前检查：不能删除当前登录用户、不能删除最后一个租户管理员、不能删除平台内置管理员。
- 用户详情增加角色列表、会话状态、最近登录信息。
- 用户唯一性从当前全局用户名逐步调整为租户内唯一；如不调整数据库唯一索引，需明确保留全局唯一约束。

### 6.5 角色管理

#### 参考项目特征

- 角色编码和角色名称唯一性检查。
- 删除前检查是否已有用户绑定。
- 角色菜单授权单独入口。
- 权限码粒度按 `page/get/add/edit/del/update` 展开。

#### 改造项

- 角色列表改为支持分页、关键字、状态/数据范围过滤。
- 角色表单突出角色编码、角色名称、数据范围、描述。
- 授权弹窗使用菜单树，支持半选、全选、展开全部、只看已选。
- 删除角色前检查用户绑定，提示绑定用户数量。
- `PermissionCodes` 继续保持统一登记，但菜单节点的 `grant_key` 要从粗粒度逐步拆到页面动作级别。
- 角色授权保存后刷新权限快照，保证当前用户感知一致。

### 6.6 部门管理

#### 参考项目特征

`sys_dept` 包含部门名称、负责人、负责人电话、排序、状态、租户、父级。

#### 改造项

- `sys_dept` 补齐：负责人名称、负责人电话、排序、状态。
- 当前 `leader_user_id` 保留，新增展示层可同时显示负责人用户和手填负责人。
- 部门树支持拖拽或上移/下移排序。
- 删除部门前检查：子部门、用户引用。
- 用户管理页和部门管理页共用部门树数据。
- 新建租户时默认生成根部门。

### 6.7 租户套餐

#### 当前优势

本项目已经有“套餐 + 能力 + 能力资源范围”，比参考项目单纯 `app_key` 更可维护。

#### 参考项目对齐

参考项目套餐字段更适合运营展示：名称、子标题、销售价、原价、描述、应用 key、状态。

#### 改造项

- `sys_tenant_package` 补齐运营字段：子标题、销售价、原价、富文本/Markdown 描述、展示排序。
- 保留 `package_code`，增加 `app_key` 作为兼容展示字段或由能力集合自动派生。
- 套餐表单分为三段：基础信息、价格展示、能力/应用范围。
- 套餐详情展示“包含能力”和“影响菜单”。
- 删除套餐前做影响分析：已绑定租户数量、受影响能力、受影响菜单。
- 租户创建/编辑时套餐选择器展示套餐摘要，而不是只展示编码。

### 6.8 代码生成

#### 当前问题

本项目已有白名单、预览、生成、下载、自定义模板，但参考项目有完整数据源和字段配置持久化。

#### 目标结构

```text
codegen_data_source
  ├─ name
  ├─ jdbc_url
  ├─ username
  ├─ password_cipher
  ├─ db_name
  ├─ host
  └─ port

codegen_table
  ├─ table_name
  ├─ table_comment
  ├─ class_name
  ├─ tpl_category
  ├─ package_name
  ├─ module_name
  ├─ business_name
  ├─ function_name
  ├─ function_author
  ├─ gen_type
  ├─ gen_path
  └─ options

codegen_table_column
  ├─ column_name
  ├─ column_comment
  ├─ column_type
  ├─ java_type
  ├─ java_field
  ├─ is_pk / is_required / is_insert / is_edit / is_list / is_query
  ├─ query_type
  ├─ html_type
  ├─ dict_type
  └─ sort
```

#### 改造项

- 新增数据源管理页：列表、新增、编辑、测试连接、删除。
- 数据表列表支持按数据源切换，不只依赖当前应用库。
- 增加“导入表配置”：将表和字段元数据保存到 `codegen_table/codegen_table_column`。
- 表配置页支持字段级配置：列表显示、查询方式、表单控件、字典类型、必填、排序。
- 模板管理继续保留现有能力，但模板变量向参考项目的 `vm/java`、`vm/ts`、`vm/vue`、`vm/xml` 分类靠齐。
- 生成结果保留预览、下载、隔离目录输出，不直接覆盖源码。
- 白名单继续作为安全边界；外部数据源必须经过白名单或显式授权。

## 7. 前端整体改造

### 7.1 菜单分组建议

当前本项目有“系统管理”和“平台管理”。建议调整为：

```text
系统管理
  ├─ 用户管理
  ├─ 角色管理
  ├─ 部门管理
  ├─ 菜单管理
  ├─ 字典管理
  └─ 操作日志

平台管理
  ├─ 租户管理
  ├─ 租户套餐
  ├─ 代码生成
  ├─ 文件管理
  └─ 邮件配置
```

### 7.2 页面交互统一

| 页面类型 | 推荐形态 |
|---|---|
| 树型模块 | 左树右表，或树表一体 |
| 授权模块 | 树形勾选弹窗，显示半选状态 |
| 字典模块 | 左侧字典类型，右侧字典值 |
| 租户/用户/角色 | 查询区 + 表格 + 弹窗表单 + 行操作 |
| 代码生成 | 数据源 Tabs + 表列表 + 字段配置 + 预览抽屉 |

## 8. 权限码改造

当前 `PermissionCodes` 多数是 `read/write` 粗粒度。参考项目按 `page/get/add/edit/del` 拆分。

建议采用两阶段：

### 阶段一：页面操作粒度补菜单节点

先不强制改 Controller 权限码，只在 `sys_menu` 中补充按钮节点：

```text
user:read      用户查看
user:create    用户新增
user:update    用户修改
user:delete    用户删除
user:password  用户重置密码

role:read      角色查看
role:create    角色新增
role:update    角色修改
role:delete    角色删除
role:grant     角色授权
```

### 阶段二：后端权限码细化

当页面按钮和角色授权稳定后，再把 Controller 从 `read/write` 逐步拆到动作级权限码。

## 9. 数据库迁移计划

新增 Flyway 脚本，不修改旧脚本。脚本按“基础字段 → 语义收敛 → 菜单权限 → 代码生成元数据 → 初始化数据”的顺序执行，避免运行期权限链路中断。

### 9.1 迁移脚本清单

| 脚本 | 内容 | 影响范围 | 前置条件 |
|---|---|---|---|
| `V202606080001__upms_profile_fields.sql` | 租户、部门、套餐补齐运营字段 | `sys_tenant`、`sys_dept`、`sys_tenant_package` | 当前基础表存在 |
| `V202606080002__dict_type_cleanup.sql` | 字典类型和字典值语义收敛，迁移旧字典项数据 | `sys_dict`、`sys_dict_value` | `V202606070003__dict_value_table.sql` 已执行 |
| `V202606080003__menu_action_permissions.sql` | 补充按钮/API 权限节点和默认角色授权 | `sys_menu`、`sys_role_menu` | `V202606070001__split_menu_permission.sql` 已执行 |
| `V202606080004__tenant_package_resource_scope.sql` | 套餐能力与菜单范围映射补齐 | `sys_tenant_capability_resource_scope`、套餐能力关系表 | `V202606060001__capability_resource_scope.sql` 已执行 |
| `V202606080005__codegen_metadata_tables.sql` | 新增数据源、表配置、字段配置元数据表 | `codegen_data_source`、`codegen_table`、`codegen_table_column` | 当前代码生成模块可用 |
| `V202606080006__module_refactor_seed_permissions.sql` | 初始化新增页面、按钮权限和菜单分组 | `sys_menu`、`sys_role_menu` | 前序菜单权限脚本已执行 |

### 9.2 字段补齐明细

| 表 | 新增/收敛字段 | 默认值策略 | 约束策略 |
|---|---|---|---|
| `sys_tenant` | `logo_url`、`logo_file_key`、`address`、`site_url`、`contact_phone`、`contact_email`、`auth_begin_time` | 旧数据允许为空；`auth_begin_time` 可用创建时间回填 | 联系方式只做格式校验，不做唯一约束 |
| `sys_dept` | `leader_name`、`leader_phone`、`order_no`、`enabled` | `order_no = 0`，`enabled = 1` | 删除仍走引用检查，不做物理级联 |
| `sys_tenant_package` | `subtitle`、`sales_price`、`original_price`、`description_md`、`app_key`、`order_no` | 价格为空表示不展示；`order_no = 0` | `package_code` 保持唯一 |
| `sys_dict` | `dict_type`、`description`、`enabled`、`remarks` | 旧 `dict_code` 回填到 `dict_type` | `tenant_id + dict_type` 唯一 |
| `sys_dict_value` | `dict_id`、`dict_type`、`dict_label`、`dict_value`、`sort`、`show_class`、`enabled`、`remarks` | 旧字典项迁移为 value 行 | `dict_id + dict_value` 唯一 |

### 9.3 数据迁移规则

```text
旧字典数据
  ├─ sys_dict.dict_code -> sys_dict.dict_type
  ├─ sys_dict.dict_value -> sys_dict_value.dict_value
  └─ sys_dict.dict_name / description -> sys_dict_value.dict_label 或 sys_dict.description

菜单权限数据
  ├─ 保留现有 DIR / MENU 节点
  ├─ 为核心页面补 BUTTON 节点
  ├─ grant_key 作为按钮权限标识
  └─ 平台管理员角色默认获得新增按钮权限

租户菜单范围
  ├─ 套餐能力决定默认 VISIBLE / GRANT 范围
  ├─ 租户覆盖能力只保存差异
  └─ 运行期仍从能力资源范围过滤菜单树
```

### 9.4 迁移后检查 SQL

迁移脚本完成后至少检查以下结果：

```sql
-- 菜单权限节点不能缺失权限标识
select id, menu_name, menu_type
from sys_menu
where deleted = 0 and menu_type in ('BUTTON', 'API') and (grant_key is null or grant_key = '');

-- 字典类型不能重复
select tenant_id, dict_type, count(*)
from sys_dict
where deleted = 0
group by tenant_id, dict_type
having count(*) > 1;

-- 字典值不能脱离字典类型
select v.id, v.dict_type, v.dict_value
from sys_dict_value v
left join sys_dict d on d.id = v.dict_id and d.deleted = 0
where v.deleted = 0 and d.id is null;

-- 部门不能形成无效父级
select d.id, d.parent_id
from sys_dept d
left join sys_dept p on p.id = d.parent_id and p.deleted = 0
where d.deleted = 0 and d.parent_id is not null and p.id is null;
```

## 10. 后端接口改造清单

接口保持现有模块边界，不新增跨模块大服务。跨模块场景通过应用服务编排，避免 Controller 直接拼装多模块数据。

### 10.1 菜单管理

| 能力 | 接口形态 | 说明 |
|---|---|---|
| 菜单树查询 | `GET /api/platform/menus/tree` | 返回目录、菜单、按钮、API 统一树 |
| 菜单保存 | `POST /api/platform/menus`、`PUT /api/platform/menus/{id}` | 按节点类型校验必填字段 |
| 菜单删除 | `DELETE /api/platform/menus/{id}` | 删除前检查子节点、角色引用、能力范围引用 |
| 可授权树 | `GET /api/platform/menus/grantable-tree` | 根据租户能力范围过滤 |
| 批量按钮权限 | `POST /api/platform/menus/{id}/actions` | 按页面动作模板生成按钮节点 |

### 10.2 字典管理

| 能力 | 接口形态 | 说明 |
|---|---|---|
| 字典类型分页 | `GET /api/system/dicts` | 返回字典值数量和更新时间 |
| 字典详情 | `GET /api/system/dicts/{id}` | 一次返回字典类型和字典值列表 |
| 字典类型保存 | `POST /api/system/dicts`、`PUT /api/system/dicts/{id}` | `dict_type` 在租户内唯一 |
| 字典值保存 | `POST /api/system/dicts/{id}/values`、`PUT /api/system/dict-values/{valueId}` | 支持排序、样式、启停 |
| 缓存刷新 | `DELETE /api/system/dicts/cache` | 刷新字典缓存，前端刷新成功后重新加载当前数据 |

### 10.3 用户、角色、部门

| 模块 | 新增/调整接口 | 关键规则 |
|---|---|---|
| 用户 | 部门树过滤、重置密码、强制改密、用户详情 | 角色必选；管理员删除保护；租户内唯一策略明确 |
| 角色 | 分页查询、授权保存、影响分析、删除检查 | 删除前返回绑定用户数量；授权后刷新权限快照 |
| 部门 | 部门树、排序、启停、删除检查 | 有子部门或用户引用时禁止删除 |

### 10.4 租户与套餐

| 能力 | 接口形态 | 说明 |
|---|---|---|
| 租户资料保存 | `POST /api/platform/tenants`、`PUT /api/platform/tenants/{id}` | 包含 Logo、地址、官网、联系人、授权期限 |
| 租户初始化 | 创建租户流程内部完成 | 根部门、管理员、管理员角色、默认授权一次性落地 |
| 租户能力详情 | `GET /api/platform/tenants/{id}/capability-summary` | 返回套餐默认、租户覆盖、实际菜单范围 |
| 套餐详情 | `GET /api/platform/tenant-packages/{code}` | 返回能力集合和影响菜单 |
| 套餐删除影响 | `GET /api/platform/tenant-packages/{code}/impact` | 返回绑定租户数量和资源影响 |

### 10.5 代码生成

| 能力 | 接口形态 | 说明 |
|---|---|---|
| 数据源管理 | `GET/POST/PUT/DELETE /api/platform/codegen/datasources` | 密码加密保存，删除前检查导入表配置 |
| 测试连接 | `POST /api/platform/codegen/datasources/{id}/test` | 只返回连接状态和错误摘要 |
| 表元数据读取 | `GET /api/platform/codegen/datasources/{id}/tables` | 受白名单或显式授权约束 |
| 导入表配置 | `POST /api/platform/codegen/tables/import` | 保存表与字段元数据 |
| 字段配置保存 | `PUT /api/platform/codegen/tables/{id}/columns` | 影响预览、下载、生成结果 |

## 11. 前端落地清单

### 11.1 页面入口调整

```text
系统管理
  ├─ 用户管理
  ├─ 角色管理
  ├─ 部门管理
  ├─ 菜单管理
  ├─ 字典管理
  └─ 操作日志

平台管理
  ├─ 租户管理
  ├─ 租户套餐
  ├─ 代码生成
  ├─ 文件管理
  └─ 邮件配置
```

### 11.2 页面改造矩阵

| 页面 | 改造重点 | 状态来源 | 关键交互 |
|---|---|---|---|
| `MenuManagementView.vue` | 树表字段补齐、节点类型动态表单、按钮权限批量生成 | `sys_menu` | 复制权限标识、删除引用提示 |
| `SystemDictsView.vue` | 左字典类型、右字典值 | `sys_dict + sys_dict_value` | 类型切换刷新、字典值排序、样式预览 |
| `TenantsView.vue` | 租户资料、授权期限、套餐选择、状态提示 | `sys_tenant` | 创建后初始化进度、到期状态标识 |
| `UsersView.vue` | 部门树过滤、角色必选、重置密码、强制改密 | `sys_user + sys_dept + sys_role` | 管理员保护提示、密码策略提示 |
| `RolesView.vue` | 分页、授权树增强、影响分析 | `sys_role + sys_role_menu` | 半选、只看已选、展开全部 |
| `DepartmentsView.vue` | 负责人、电话、排序、状态 | `sys_dept` | 删除引用提示、排序调整 |
| `TenantCatalogView.vue` | 套餐运营展示、能力和菜单影响 | `sys_tenant_package + capability_scope` | 删除影响分析、套餐摘要 |
| `CodegenView.vue` | 数据源、表配置、字段配置、模板分类 | codegen 元数据表 | 数据源测试、导入表、预览抽屉 |

### 11.3 通用交互约束

- 查询区字段不超过两行，更多条件折叠。
- 表格行操作保持“查看/编辑/删除/更多”顺序。
- 删除类操作先展示影响范围，再二次确认。
- 授权树默认展示目录和菜单，按钮权限可通过开关展开。
- 所有启停状态使用一致的视觉标识和文案。
- 表单提交后刷新当前页数据，不强制回到第一页。

## 12. 实施阶段与检查点

### P0：基线确认

- 确认当前登录、权限快照、菜单渲染、角色授权可用。
- 导出当前 `sys_menu`、`sys_role_menu`、`sys_tenant_package`、`sys_tenant_capability_resource_scope` 基线数据。
- 固化菜单和权限主链路：只允许 `sys_menu + sys_role_menu` 参与运行时授权。
- 确认前端当前路由和后端权限码一一对应，不存在死菜单入口。

验收点：

- 登录后菜单树稳定渲染。
- 角色授权保存后重新登录可生效。
- 旧 `sys_permission/sys_role_permission` 不参与运行时授权。

### P1：菜单、角色、用户、部门

- 菜单页面补齐参考字段和节点类型表单。
- 角色授权树增强，补齐删除前引用检查。
- 用户页面补齐部门树、角色必选、重置密码、管理员保护。
- 部门补齐负责人、电话、排序、状态。
- 补充核心页面按钮权限节点。

验收点：

- 新建菜单后能出现在菜单管理树中。
- 给角色授权后，用户重新登录能获得对应菜单。
- 删除被用户绑定的角色会被拦截。
- 删除有用户或子部门的部门会被拦截。
- 按钮权限能控制页面关键操作入口。

### P2：字典与租户套餐

- 字典改为左类型右字典值。
- 字典值支持排序、样式、启停。
- 套餐补齐运营展示字段。
- 套餐详情展示能力与菜单范围。
- 租户编辑时能选择套餐并触发能力范围变化。

验收点：

- 字典类型和字典值边界清晰，不再混用。
- 字典缓存刷新后页面能立即展示新值。
- 租户套餐变化能影响租户可见菜单范围。
- 套餐删除前能展示影响租户数量。

### P3：租户管理

本轮状态：租户主档资料、授权期限和初始化链路已完成；能力详情增强仍留作后续小阶段。

已完成：

- 租户资料补齐 Logo、地址、官网、联系信息。
- 授权起止时间补齐：新增授权开始时间，继续沿用过期时间作为授权结束时间。
- 登录与租户切换边界接入租户可用性校验：停用、未到授权开始、已过授权结束均会被拦截。
- 租户列表、详情、目录视图、变更历史、前端表单和 E2E mock 已同步主档字段。
- 创建租户时自动初始化根部门、`TENANT_ADMIN` 管理员角色、租户管理员用户、默认菜单授权和强制首次改密状态。

后续独立小阶段：

- 租户详情继续增强能力覆盖、套餐能力、实际菜单范围展示。

验收点：

- 停用租户不可登录或不可切换。
- 未到授权开始时间的租户不可登录或不可切换。
- 过期租户不可登录或不可切换，并有明确状态提示。
- 租户主档资料可在列表、详情、编辑表单和目录视图稳定回显。
- 新租户创建后会生成根部门、管理员角色、管理员用户和默认授权。
- 租户能力覆盖后菜单范围可验证，留到能力详情增强小阶段验收。

### P4：代码生成

本轮状态：代码生成元数据化主链路已完成；外部数据源真实连接按安全策略保持关闭，待显式授权后再启用。

已完成：

- 新增 `codegen_data_source`、`codegen_table`、`codegen_table_column` 元数据表。
- 新增数据源管理接口：列表、新增、编辑、删除、测试连接。
- 当前应用库作为默认本地数据源，可读取白名单范围内的数据表。
- 新增表配置导入和字段配置持久化接口。
- 字段配置可覆盖 Java 类型、Java 字段、注释、查询方式、表单控件、字典类型和排序。
- 预览、生成、下载继续走隔离目录和白名单边界。
- 前端 API 和类型已接入数据源、导入表、字段配置主链路。

后续独立小阶段：

- 外部 JDBC 数据源真实连接、凭据加密和显式授权开关。
- 页面级完整字段配置编辑器和数据源管理交互增强。
- 模板分类从现有 `java/typescript/vue` 继续细化到 Java/XML/TS/Vue。

验收点：

- 当前应用库数据源测试连接成功后可读取表列表。
- 导入表后能编辑字段生成配置。
- 字段配置变化能体现在预览和下载结果中。
- 预览和下载内容一致。
- 非白名单表不能生成。

## 13. 测试与回归范围

### 13.1 后端回归

| 模块 | 必测链路 |
|---|---|
| 登录与权限 | 登录、登出、权限快照刷新、租户切换、菜单过滤 |
| 菜单 | 创建、编辑、删除检查、授权树、按钮权限节点 |
| 角色 | 分页、唯一性校验、授权保存、绑定用户删除拦截 |
| 用户 | 部门过滤、角色必选、重置密码、强制改密、管理员保护 |
| 部门 | 树查询、排序、启停、子部门/用户引用删除拦截 |
| 字典 | 类型 CRUD、字典值 CRUD、缓存刷新、样式回显 |
| 租户 | 创建初始化、套餐变更、启停、过期状态、能力覆盖 |
| 套餐 | 运营字段、能力范围、影响分析、删除拦截 |
| 代码生成 | 数据源测试、导入表、字段配置、预览、下载、白名单拦截 |

### 13.2 前端回归

- 菜单渲染：平台管理员、租户管理员、普通用户三类账号分别验证。
- 授权树：全选、半选、取消、只看已选、展开全部。
- 表单校验：必填、唯一性、格式、状态切换。
- 删除影响：角色、部门、套餐、菜单都必须展示清晰影响。
- 字典回显：启停、排序、样式、缓存刷新后展示一致。
- 代码生成：字段配置改变后，预览和下载结果一致。

### 13.3 最小自动化覆盖

| 层级 | 建议用例 |
|---|---|
| 单元测试 | 删除引用检查、租户初始化、字典迁移映射、代码生成字段映射 |
| 接口测试 | 菜单授权、角色授权、用户重置密码、套餐影响分析、数据源测试 |
| 前端测试 | 授权树半选、字典左右联动、租户套餐选择、代码生成预览 |

## 14. 回滚与灰度策略

- 数据库只新增字段和表，不删除旧字段；旧字段至少保留一个版本周期。
- 字典语义收敛先兼容旧读取，再迁移页面和接口。
- 菜单按钮权限先补数据，不立即强制所有 Controller 改细粒度权限码。
- 代码生成外部数据源默认关闭，由配置开关或平台管理员显式启用。
- 租户初始化流程保留幂等检查，创建失败时可按租户维度清理派生数据。
- 每个阶段完成后单独发布，确认登录、菜单、角色授权无回归后再进入下一阶段。

## 15. 风险与处理

| 风险 | 处理方式 |
|---|---|
| 参考项目权限码命名和本项目不一致 | 保留本项目 `PermissionCodes`，只吸收动作粒度设计 |
| 租户套餐模型差异较大 | 不退化为 `app_key`，用能力集合生成展示层应用标识 |
| 字典旧数据语义混乱 | 先兼容读取，再迁移到二级模型 |
| 代码生成外部数据源带来安全风险 | 数据源加密保存，生成表必须白名单或显式授权 |
| 菜单改造影响登录权限快照 | 菜单数据迁移后必须跑登录、租户切换、角色授权回归 |
| 租户初始化涉及多模块写入 | 使用事务和幂等键，失败时按租户维度补偿 |
| 前端页面一次性改造范围过大 | 按 P1-P4 分阶段上线，优先保证运行时权限链路稳定 |

## 16. 最终交付物

- 数据库迁移脚本和迁移后检查 SQL。
- 后端接口调整与核心链路测试。
- 前端管理页改造与交互回归。
- 初始化菜单、按钮权限和角色授权数据。
- 核心链路验收记录：登录、菜单渲染、角色授权、租户创建、套餐变更、代码生成。
- 回滚说明：每阶段新增对象、兼容字段、可关闭开关和数据清理范围。

## 17. 当前执行进度

### 17.1 已完成

| 阶段 | 模块 | 完成内容 | 验证 |
|---|---|---|---|
| P1 | 部门管理 | `sys_dept` 补齐负责人姓名、负责人电话、排序、启停状态；后端创建/编辑/目录返回接入；前端表格、详情、表单接入运营字段 | `mvn "-Dtest=DeptManagementServiceTest,DeptControllerTest" test` 通过；`npm run build` 通过 |
| P1 | 菜单管理 | 菜单树表格补齐类型、权限标识、路由标识、组件、外链、应用标识、启用状态；表单补齐外链和应用标识；新增复制权限标识入口；新增批量生成按钮权限接口与页面入口；删除菜单前增加角色授权引用检查 | `mvn "-Dtest=ResourceAuthorizationControllerTest" test` 通过；`npm run build` 通过 |
| P1 | 角色管理 | 保留客户端分页与数据范围展示；新增角色删除影响分析接口，返回绑定用户数、授权节点数和阻断提示；删除前先展示影响，命中用户绑定时阻断；租户内统计角色用户引用，避免跨租户误判 | `mvn "-Dtest=RoleManagementServiceTest" test` 通过；`npm run build` 通过 |
| P1 | 用户管理 | 补齐部门树过滤、角色必选、重置密码触发强制改密、管理员/当前用户保护；后端按数据权限校验部门和用户访问，服务层阻断删除当前用户、停用当前用户和清空当前用户角色；前端同步提示密码策略和保护规则 | `mvn "-Dtest=UserManagementServiceTest,UserControllerTest" test` 通过；`npm run build` 通过 |
| P2 | 字典管理 | 后端收敛为“字典类型 + 字典值”二级模型；新增字典详情接口和字典值 CRUD；字典类型返回值数量、启停、备注、更新时间；前端改为左字典类型、右字典值管理，支持排序、回显样式、启停和缓存刷新后重载 | `mvn "-Dtest=SystemControllerTest" test` 通过；`npm run build` 通过 |
| P2 | 租户套餐 | 补齐套餐运营字段、应用标识、展示排序、价格和详细描述；套餐列表/详情展示能力集合、可见/授权资源影响和引用租户；新增套餐/能力影响分析接口，删除前按阻断规则拦截 | `mvn "-Dtest=TenantCatalogControllerTest" test` 通过；`npm run build` 通过 |
| P3 | 租户管理 | 补齐租户 Logo、官网、地址、联系人、联系方式、授权开始时间；继续沿用过期时间作为授权结束时间；创建、编辑、列表、详情、目录视图、变更历史和前端表单已接入；登录与租户切换边界已拦截停用、未开始授权和已过期租户 | `mvn "-Dtest=TenantControllerTest,AuthControllerSessionFlowTest,SystemControllerTest,DictApplicationServiceTest,AuthControllerRegisterTest,ModuleBoundaryTest" test` 通过；`mvn -q -DskipTests compile` 通过；`npm run build` 通过；`mvn test` 全量通过 |
| P3 | 租户初始化链路 | 新增跨模块 `TenantBootstrapFacade`；创建租户后同一事务内幂等初始化根部门、`TENANT_ADMIN` 管理员角色、租户管理员用户、用户角色关系和租户能力范围内默认菜单授权；管理员账号初始密码写入哈希并强制首次改密；变更历史记录初始化摘要 | `mvn "-Dtest=TenantControllerTest" test` 通过 |
| P4 | 代码生成元数据化 | 新增 `codegen_data_source/codegen_table/codegen_table_column`；新增数据源管理、当前库表读取、导入表配置、字段配置保存接口；生成模型会读取已导入字段配置并影响预览/生成/下载；前端 API 与类型已接入数据源、导入表和字段配置主链路 | `mvn "-Dtest=CodegenApplicationServiceP2Test" test` 通过；`npm run build` 通过 |
| 体验增强 | 代码生成页面 | 在 `CodegenView.vue` 增强数据源选择、连接测试、表格多选导入、已导入配置列表、字段配置抽屉；字段配置保存后标记预览过期并提示重新预览，形成“选表 → 导入 → 配字段 → 预览/生成”的页面闭环 | `npm run build` 通过 |
| 体验增强 | 租户能力详情 | 租户能力摘要接口返回套餐默认能力、覆盖后生效能力、差异数量、实际可见菜单和实际可授权菜单；租户详情抽屉分块展示能力来源、覆盖差异和菜单授权边界 | `mvn "-Dtest=TenantControllerTest" test` 通过；`npm run build` 通过 |
| 体验增强 | 外部代码生成数据源授权 | 新增数据源授权状态字段和迁移；本地库默认授权，外部数据源默认待授权；新增显式授权接口和前端确认入口，未授权外部源不能测试连接、读取表结构或导入配置 | `mvn "-Dtest=CodegenApplicationServiceP2Test" test` 通过；`npm run build` 通过 |
| 体验增强 | 代码生成模板分类 | 新增模板分类字段与迁移，支持 backend/frontend/api/type/view 分类；模板列表接口支持分类筛选；前端模板页增加分类筛选、分类标签和编辑表单分类选择 | `mvn "-Dtest=CodegenApplicationServiceP2Test" test` 通过；`npm run build` 通过 |
| 体验增强 | 关键管理页细节打磨 | 租户页增加数据刷新时间和手动刷新入口；租户套餐/能力页增加统一刷新入口和刷新时间；代码生成页增加操作顺序提示，明确“选源、选表、导入、配字段、预览、生成/下载”的闭环 | `npm run build` 通过 |

### 17.2 执行备注

- 菜单批量按钮权限验证中，接口测试已切换为测试内存会话，避免权限快照清理依赖外部 Redis 状态。
- 字典阶段已落地 `V202606080002__dict_type_cleanup.sql`，本次测试中 Flyway 已从 `202606080001` 升级到 `202606080002`。
- `SystemControllerTest` 已补充字典详情、字典值创建、修改、删除的接口级回归。
- 前端构建存在既有大 chunk 提示，不影响本阶段通过状态。
- 当前仍按“模块化单体 + 管理后台”推进，不引入参考项目的微服务拆分方式。


当前模块改造主线与体验增强已完成，后续建议进入整体验收、回归清单执行和发布前缺陷收敛。