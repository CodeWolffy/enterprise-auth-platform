# enterprise-auth-platform 使用说明文档

更新时间：2026-03-24

## 1. 这套系统完善了吗？

结论：**核心链路已完整可用**，适合本地开发、联调、功能演示和内网试运行；若要正式生产上线，还需要按部署清单做安全加固与环境参数收敛。

已确认的依据：
- 后端编译通过：`mvn "-Dmaven.repo.local=.m2repo" -DskipTests compile`
- 前端构建通过：`cd frontend && npm run build`
- 代码中已覆盖：认证授权、OAuth2 客户端/作用域、多租户、RBAC、审计导出、系统配置管理
- 仓库自带部署检查清单：`DEPLOYMENT_CHECKLIST.md`

## 2. 系统组成

- 后端：Spring Boot（默认 `http://127.0.0.1:8080`）
- 前端：Vue3 + Vite（默认 `http://127.0.0.1:5173`）
- 数据库：MySQL 8
- 缓存：Redis

## 3. 环境准备

请先安装并确认：
- JDK 17
- Maven 3.9+
- Node.js 20+
- MySQL 8.0
- Redis 6+

## 4. 5 分钟快速启动

### 4.1 创建数据库并导入初始化脚本

```sql
CREATE DATABASE IF NOT EXISTS enterprise_auth_platform
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

执行脚本：
- `src/main/resources/database/enterprise_auth_platform.sql`

### 4.2 检查后端配置

文件：`src/main/resources/application.yml`

默认配置（本地）：
- MySQL：`127.0.0.1:3306`，用户名 `root`，密码 `123456`
- Redis：`127.0.0.1:6379`
- 后端端口：`8080`
- 前端允许来源：`http://127.0.0.1:5173`

### 4.3 启动后端

在项目根目录执行：

```bash
mvn spring-boot:run
```

健康检查：
- 打开 `http://127.0.0.1:8080/actuator/health`
- 返回 `UP` 即正常

### 4.4 启动前端

```bash
cd frontend
npm install
npm run dev
```

浏览器打开：
- `http://127.0.0.1:5173`

## 5. 首次登录

系统默认走 OAuth2 授权码 + PKCE 登录流程，会自动跳到统一登录页。

初始化脚本内置账号：
- 平台租户：`platform`
- 管理员账号：`admin`
- 默认密码：`123456`

可选测试账号：
- 租户：`tenant-a`
- 账号：`auditor`
- 密码：`123456`

## 6. 新手使用路径（推荐顺序）

### 第一步：先确认租户
- 左下角查看“当前租户”
- 平台管理建议先使用 `platform` 租户

### 第二步：用户、角色、权限
1. 在“权限管理”先建权限码（如 `user:read`）
2. 在“角色管理”创建角色并分配权限
3. 在“用户管理”创建用户并绑定角色
4. 用新账号登录验证菜单和数据范围

### 第三步：组织与租户
1. 在“部门管理”维护组织树
2. 在“租户管理”维护租户状态、套餐、能力覆盖
3. 需要时在“租户目录”维护套餐/能力并做影响分析

### 第四步：OAuth2 对接
1. 在“OAuth2 客户端”创建/维护客户端
2. 在“OAuth2 作用域”维护 scope
3. 通过联动分析检查 client 和 scope 影响面

### 第五步：审计与治理
1. 在“安全审计”按条件检索事件
2. 创建导出任务并下载
3. 配置导出保留策略（支持 dryRun 预演）

## 7. 常用接口速查

- 登录相关：`/api/auth/*`
- OAuth2 客户端：`/api/oauth-clients/*`
- OAuth2 作用域：`/api/oauth-scopes/*`
- 用户/角色/权限：`/api/users`、`/api/roles`、`/api/permissions`
- 审计：`/api/audit/*`
- 租户：`/api/tenants`、`/api/tenant-catalog/*`

## 8. 常见问题

### Q1：前端一直跳登录或 401
- 检查后端是否启动在 `8080`
- 检查浏览器访问是否为 `127.0.0.1:5173`（与 allowed-origins 一致）
- 清理浏览器 Cookie 后重试

### Q2：登录成功但菜单空白
- 当前账号无菜单权限
- 在“角色管理”给角色分配权限后重新登录

### Q3：无法切换租户
- 只有平台超级管理员可跨租户操作
- 普通租户用户会被限制在所属租户

### Q4：生产环境最容易漏什么
- `issuer` 必须改为正式 HTTPS 域名
- `jwt-secret`、数据库密码、前端 client secret 必须替换
- 仅保留真实 `allowed-origins` 和回调地址白名单

## 9. 上线前必做

请逐项执行：
- `DEPLOYMENT_CHECKLIST.md`

重点项：
- 安全密钥替换
- CORS 与回调地址白名单
- 租户头透传与防注入
- 认证/审计链路冒烟测试

