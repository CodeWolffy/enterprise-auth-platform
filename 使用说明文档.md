# enterprise-auth-platform 使用说明文档

更新时间：2026-03-27

## 1. 系统概述

企业级权限管理平台是一套完整的身份认证与授权管理解决方案，提供 OAuth2/OIDC 认证能力、基于 RBAC 的权限管理、多租户隔离、审计日志等功能。

核心能力：
- OAuth2 / OIDC 授权码模式认证
- RBAC（角色-Based 访问控制）
- 多租户支持（含平台租户）
- OAuth2 客户端管理（启用/停用、密钥轮换）
- OAuth2 作用域管理
- 审计日志查询与导出
- 系统配置管理（字典、配置、公告、分类）

## 2. 系统组成

| 组件 | 技术栈 | 默认地址 |
|------|--------|----------|
| 后端 | Spring Boot 3.2.5 + Spring Authorization Server | http://127.0.0.1:8080 |
| 前端 | Vue3 + Vite + Element Plus | http://127.0.0.1:5173 |
| 数据库 | MySQL 8.0 | 127.0.0.1:3306 |
| 缓存 | Redis | 127.0.0.1:6379 |

## 3. 环境准备

### 3.1 必需软件

| 软件 | 版本要求 | 验证命令 |
|------|----------|----------|
| JDK | 17+ | `java -version` |
| Maven | 3.9+ | `mvn -version` |
| Node.js | 20+ | `node -v` |
| MySQL | 8.0+ | `mysql --version` |
| Redis | 6+ | `redis-cli ping` |

### 3.2 环境变量（如有必要）

```bash
# MySQL（如非默认端口）
export MYSQL_PORT=3306

# Redis（如需认证）
export REDIS_PASSWORD=your_password

# 运行时指定 Spring Profile
export SPRING_PROFILES_ACTIVE=dev
```

## 4. 5 分钟快速启动

### 4.1 创建数据库并导入初始化脚本

```sql
CREATE DATABASE IF NOT EXISTS enterprise_auth_platform
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

执行初始化脚本：
```bash
mysql -u root -p enterprise_auth_platform < src/main/resources/database/enterprise_auth_platform.sql
```

### 4.2 检查后端配置

配置文件：`src/main/resources/application.yml`

默认本地配置：
- MySQL：`127.0.0.1:3306`，用户名 `root`，密码 `123456`
- Redis：`127.0.0.1:6379`
- 后端端口：`8080`
- 前端允许来源：`http://127.0.0.1:5173`

### 4.3 启动后端

```bash
# 开发模式（推荐）
mvn spring-boot:run

# 或打包后运行
mvn clean package -DskipTests
java -jar target/enterprise-auth-platform-0.0.1-SNAPSHOT.jar
```

健康检查：
```bash
curl http://127.0.0.1:8080/actuator/health
# 返回 {"status":"UP"} 即正常
```

### 4.4 启动前端

```bash
cd frontend
npm install
npm run dev
```

浏览器访问：`http://127.0.0.1:5173`

### 4.5 构建生产版本

```bash
# 后端
mvn clean package -DskipTests

# 前端
cd frontend
npm run build
```

构建产物位于 `target/` 和 `frontend/dist/`

## 5. 首次登录

### 5.1 登录流程

系统采用 OAuth2 授权码 + PKCE 模式：
1. 访问前端页面，自动跳转到登录页
2. 输入用户名密码完成认证
3. 若涉及 OAuth2 授权，会跳转到授权同意页
4. 授权通过后完成回调，登录成功

### 5.2 初始化账号

| 角色 | 租户 | 用户名 | 密码 | 说明 |
|------|------|--------|------|------|
| 平台管理员 | platform | admin | 123456 | 超级管理员，可管理所有租户 |
| 审计员 | tenant-a | auditor | 123456 | 测试账号，仅限租户 tenant-a |

**注意**：首次登录后建议立即修改默认密码。

## 6. 功能模块使用指南

### 6.1 权限管理（RBAC）

#### 6.1.1 权限码管理
入口：权限管理 → 权限码

权限码是系统中最细粒度的权限单元，通常采用 `资源:操作` 格式：
- `user:read` - 查看用户
- `user:create` - 创建用户
- `user:update` - 修改用户
- `user:delete` - 删除用户
- `menu:execute` - 执行菜单

#### 6.1.2 角色管理
入口：角色管理 → 角色列表

创建角色的步骤：
1. 点击「新增角色」
2. 填写角色名称、编码、状态
3. 为角色分配权限码
4. 保存

#### 6.1.3 用户管理
入口：用户管理 → 用户列表

创建用户的步骤：
1. 点击「新增用户」
2. 填写用户名、姓名、手机、邮箱
3. 选择所属租户和部门
4. 为用户分配角色（可分配多个）
5. 保存

**数据范围说明**：创建用户时可设置数据权限级别：
- 全部数据权限
- 自定数据权限（指定部门及子部门）
- 仅本部门数据权限
- 仅本部门及子部门数据权限
- 仅本人数据权限

### 6.2 部门管理

入口：部门管理

功能：
- 维护组织架构树形结构
- 支持拖拽调整部门层级
- 部门负责人配置
- 部门状态启用/停用

### 6.3 租户管理

入口：租户管理

平台支持多租户隔离，租户类型：
- **platform**：平台级租户，超级管理员所在租户，可管理所有其他租户
- **普通租户**：普通业务租户，用户只能访问本租户数据

租户管理操作：
- 新增租户：填写租户名称、编码、状态、套餐信息
- 编辑租户：修改租户配置
- 启用/停用租户
- 租户参数覆盖：为特定租户定制化配置

### 6.4 OAuth2 客户端管理

入口：OAuth2 客户端

OAuth2 客户端是调用本系统认证能力的第三方应用。每个客户端包含：
- 客户端 ID（client_id）
- 客户端密钥（client_secret）
- 授权类型（authorization_code、refresh_token 等）
- 重定向 URI 白名单
- 允许的作用域（scope）

#### 6.4.1 创建 OAuth2 客户端
1. 点击「新增客户端」
2. 填写客户端信息：
   - 客户端名称、客户端 ID
   - 授权类型（默认授权码+刷新令牌）
   - 重定向 URI（必须是完整的 HTTPS 地址）
   - 允许的作用域
   - 令牌有效期
3. 保存后系统生成客户端密钥

#### 6.4.2 密钥轮换
已创建的客户端支持密钥轮换：
1. 选择目标客户端
2. 点击「轮换密钥」
3. 系统生成新密钥，旧密钥立即失效
4. 务必保存新密钥，过期将无法使用

#### 6.4.3 客户端状态管理
- 启用：客户端可正常发起 OAuth2 流程
- 停用：客户端无法登录，会返回 invalid_client

### 6.5 OAuth2 作用域管理

入口：OAuth2 作用域

作用域（Scope）是 OAuth2 授权中的权限单元，用于控制客户端可访问的资源范围。

系统预置作用域：
- `openid`：OIDC 身份标识
- `profile`：用户基本信息
- `email`：用户邮箱信息

自定义作用域：
1. 点击「新增作用域」
2. 填写作用域名称、描述
3. 关联权限码（决定通过该作用域可访问的 API）
4. 保存

### 6.6 授权记录管理

入口：授权记录

功能：
- 查询用户对各客户端的授权记录
- 撤销已授予的授权
- 查看授权时间、授权范围

### 6.7 租户目录（套餐/能力）

入口：租户目录 → 套餐管理 / 能力管理

租户目录用于定义租户可使用的功能套餐和能力集合：

#### 6.7.1 套餐管理
- 创建业务套餐（如基础版、专业版、企业版）
- 为套餐分配可用能力
- 设置套餐状态

#### 6.7.2 能力管理
- 定义系统能力（如是否支持 OAuth2、是否支持审计导出）
- 能力独立管理，可被多个套餐引用

#### 6.7.3 影响分析
- 修改套餐或能力前，使用「影响分析」功能
- 系统展示受影响的其他套餐、租户
- 辅助评估变更风险

### 6.8 系统配置管理

入口：系统管理

#### 6.8.1 字典管理
用于维护系统中可复用的枚举值：
- 字典类型（如性别、状态）
- 字典项（具体值及标签）

#### 6.8.2 参数配置
系统运行时参数：
- 登录验证码开关
- token 有效期
- 密码策略配置

#### 6.8.3 平台超级管理员配置“注册默认租户与角色”（急用）

适用场景：开放注册时，希望新用户自动落到指定租户，并自动授予一组默认角色。

前置条件：
1. 当前登录账号属于 `platform` 租户。
2. 账号具备 `system:read`、`system:write` 权限。
3. 目标租户中的默认角色已提前创建，且角色编码可用。

配置入口：
1. 进入「系统管理」→「参数管理」。
2. 新增或编辑以下 2 个参数（参数键必须完全一致）：

| 参数键 | 参数名称（建议） | 参数值示例 | 说明 |
|------|------|------|------|
| `registration.default_tenant_id` | 注册默认租户 | `tenant-a` | 新用户注册后所属租户 |
| `registration.default_role_codes` | 注册默认角色 | `AUDITOR,USER_VIEWER` | 多个角色用英文逗号分隔 |

注意事项：
1. `registration.default_role_codes` 中的角色编码必须存在于 `registration.default_tenant_id` 指定租户下。
2. 角色编码只写编码本身，不要写角色名称。
3. 逗号分隔时不要使用中文逗号。

生效与一致性说明：
1. 通过「参数管理」页面或 `/api/system/configs` 接口新增/修改参数后，注册策略缓存会自动失效，通常立即生效。
2. 若绕过系统接口直接改数据库，缓存最多可能在 TTL 窗口内延迟更新（默认 5 分钟）。

快速验证（推荐）：
1. 调用 `GET /api/auth/register/options`，确认返回：
  - `defaultTenantId` 等于你配置的租户编码
  - `defaultRoleCodes` 包含你配置的角色编码
2. 使用注册页完成一次新用户注册。
3. 在目标租户「用户管理」中查看新用户角色是否自动分配成功。

常见报错与处理：
1. 报错“存在无效的角色编码”：检查默认角色编码是否属于目标租户，是否拼写错误。
2. 注册成功但没有角色：检查 `registration.default_role_codes` 是否为空或被误删。
3. 注册落错租户：检查 `registration.default_tenant_id` 值是否正确。

回滚策略：
1. 如需临时停用默认角色分配，可将 `registration.default_role_codes` 置空。
2. 如需恢复默认行为，可删除这两个键；系统将回退到内置默认值（默认租户 `tenant-a`，默认角色为空）。

#### 6.8.4 公告管理
发布系统公告：
- 支持富文本内容
- 可设置发布日期和过期时间

#### 6.8.5 分类管理
用于给其他数据打标签：
- 支持多级分类
- 常用于组织架构以外的业务分类维度

### 6.9 安全审计

入口：安全审计

#### 6.9.1 审计日志查询
按条件检索审计事件：
- 时间范围
- 操作人
- 操作类型
- 目标租户
- 关键词

支持的审计事件类型：
- 用户登录/登出
- 权限变更
- 租户操作
- OAuth2 相关操作
- 配置变更

#### 6.9.2 审计导出
批量导出审计日志：
1. 在「安全审计」页面点击「导出」
2. 设置导出条件（时间范围、事件类型等）
3. 提交导出任务
4. 在「导出任务」列表查看进度
5. 任务完成后下载 Excel 文件

#### 6.9.3 导出任务管理
- 查看所有导出任务及状态
- 单条任务下载
- 任务重试（如失败）
- 归档历史任务
- 清理过期任务

#### 6.9.4 自动治理
系统支持自动化归档/清理：
- 按保留天数（retentionDays）自动归档
- 按最大任务数（maxTasks）自动清理
- 支持 dryRun 预演模式

### 6.10 仪表盘

入口：仪表盘

展示系统概览信息：
- 当前租户用户数
- 角色、权限统计
- 最近登录记录
- 审计事件趋势图

## 7. 常用接口速查

### 认证相关
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/auth/captcha` | GET | 获取图形验证码 |
| `/api/auth/register/options` | GET | 获取注册默认租户与默认角色配置 |
| `/api/auth/register` | POST | 用户注册（公开接口） |
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/refresh` | POST | 刷新 token |
| `/api/auth/logout` | POST | 退出登录 |
| `/api/auth/me` | GET | 获取当前用户信息 |
| `/api/auth/sessions` | GET | 获取当前用户会话列表 |
| `/api/auth/sessions/{sessionId}/offline` | POST | 强制下线指定会话 |

### OAuth2 客户端与作用域
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/oauth-clients` | GET | 查询客户端列表 |
| `/api/oauth-clients/{id}` | GET | 获取客户端详情 |
| `/api/oauth-clients/{id}/scope-linkage` | GET | 获取客户端关联的作用域 |
| `/api/oauth-clients` | POST | 创建客户端 |
| `/api/oauth-clients/{id}` | PUT | 更新客户端 |
| `/api/oauth-clients/{id}/status` | PUT | 启用/停用客户端 |
| `/api/oauth-clients/{id}/rotate-secret` | POST | 轮换客户端密钥 |
| `/api/oauth-clients/{id}` | DELETE | 删除客户端 |
| `/api/oauth-scopes` | GET | 查询作用域列表 |
| `/api/oauth-scopes/{id}/client-linkage` | GET | 获取作用域关联的客户端 |
| `/api/oauth-scopes` | POST | 创建作用域 |
| `/api/oauth-scopes/{id}` | PUT | 更新作用域 |
| `/api/oauth-scopes/{id}` | DELETE | 删除作用域 |

### 授权记录
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/auth/consents` | GET | 查询授权记录列表 |
| `/api/auth/consents` | DELETE | 撤销授权 |

### 租户管理
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/tenants` | GET | 查询租户列表 |
| `/api/tenants/{tenantId}` | PUT | 更新租户信息 |
| `/api/tenants/{tenantId}` | DELETE | 删除租户 |

### 租户目录
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/tenant-catalog/packages` | GET | 查询套餐列表 |
| `/api/tenant-catalog/packages` | POST | 创建套餐 |
| `/api/tenant-catalog/packages/{id}` | PUT | 更新套餐 |
| `/api/tenant-catalog/packages/{id}/impact` | GET | 查询套餐影响分析 |
| `/api/tenant-catalog/packages/{id}` | DELETE | 删除套餐 |
| `/api/tenant-catalog/capabilities` | GET | 查询能力列表 |
| `/api/tenant-catalog/capabilities` | POST | 创建能力 |
| `/api/tenant-catalog/capabilities/{id}` | PUT | 更新能力 |
| `/api/tenant-catalog/capabilities/{id}/impact` | GET | 查询能力影响分析 |
| `/api/tenant-catalog/capabilities/{id}` | DELETE | 删除能力 |

### 用户/角色/权限
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/users` | GET | 查询用户列表 |
| `/api/users` | POST | 创建用户 |
| `/api/users/{id}` | PUT | 更新用户 |
| `/api/users/{id}` | DELETE | 删除用户 |
| `/api/users/{id}/roles` | PUT | 分配角色 |
| `/api/roles` | GET | 查询角色列表 |
| `/api/roles` | POST | 创建角色 |
| `/api/roles/{id}` | PUT | 更新角色 |
| `/api/roles/{id}/resources` | GET | 查询角色资源授权 |
| `/api/roles/{id}/resources` | PUT | 分配角色资源授权 |
| `/api/roles/{id}` | DELETE | 删除角色 |
| `/api/resources/tree` | GET | 查询资源树 |
| `/api/resources` | POST | 创建资源 |
| `/api/resources/{resourceId}` | PUT | 更新资源 |
| `/api/resources/{resourceId}` | DELETE | 删除资源 |
| `/api/resources/{resourceId}/sort` | PUT | 调整资源排序 |
| `/api/tenants/{tenantId}/resource-overrides` | GET | 查询租户资源覆盖 |
| `/api/tenants/{tenantId}/resource-overrides` | PUT | 更新租户资源覆盖 |

### 部门管理
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/departments` | GET | 查询部门列表 |
| `/api/departments` | POST | 创建部门 |
| `/api/departments/{id}` | PUT | 更新部门 |
| `/api/departments/{id}` | DELETE | 删除部门 |

### 系统配置
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/system/dicts` | GET/POST/PUT/DELETE | 字典管理 |
| `/api/system/configs` | GET/POST/PUT/DELETE | 参数配置 |
| `/api/system/notices` | GET/POST/PUT/DELETE | 公告管理 |
| `/api/system/categories` | GET/POST/PUT/DELETE | 分类管理 |

### 审计与导出
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/audit/events` | GET | 查询审计日志 |
| `/api/audit/events/export` | GET | 导出审计日志 |
| `/api/audit/exports` | POST | 创建导出任务 |
| `/api/audit/exports` | GET | 查询导出任务列表 |
| `/api/audit/exports/{taskId}/download` | GET | 下载导出文件 |
| `/api/audit/exports/{taskId}/retry` | POST | 重试导出任务 |
| `/api/audit/exports/{taskId}/archive` | POST | 归档导出任务 |
| `/api/audit/exports/archive` | POST | 批量归档 |
| `/api/audit/exports/{taskId}` | DELETE | 删除导出任务 |
| `/api/audit/exports/policy` | GET/PUT | 导出保留策略 |
| `/api/audit/exports/governance` | POST | 自动治理（dryRun/执行） |

## 8. 项目结构

### 8.1 后端目录结构

```
src/main/java/com/enterprise/auth/platform/
├── auth/                    # 认证模块
│   ├── controller/         # 认证控制器
│   ├── service/            # 认证服务
│   ├── dto/                # 数据传输对象
│   ├── model/              # 数据模型
│   └── store/              # 会话存储
├── user/                   # 用户管理模块
│   ├── controller/
│   ├── service/
│   ├── dto/
│   ├── model/
│   └── repository/
├── role/                   # 角色管理模块
│   ├── controller/
│   ├── service/
│   └── dto/
├── dept/                   # 部门管理模块
│   ├── controller/
│   ├── service/
│   └── dto/
├── tenant/                 # 租户管理模块
│   ├── controller/
│   ├── service/
│   ├── dto/
│   └── TenantFilter.java  # 租户上下文过滤器
├── system/                 # 系统配置模块
│   ├── controller/
│   ├── service/
│   └── dto/
├── security/               # 安全相关
│   ├── JwtAuthenticationFilter.java
│   ├── SecurityConfig.java
│   ├── DataScopeService.java
│   └── ...
├── config/                 # 配置类
│   ├── CorsConfig.java
│   ├── AuthorizationServerConfig.java
│   ├── RedisCacheConfig.java
│   └── ...
└── persistence/            # 持久化层
    ├── repository/
    └── mapper/
```

### 8.2 前端目录结构

```
frontend/src/
├── api/                    # API 接口定义
├── assets/                 # 静态资源
├── components/             # 公共组件
├── composables/            # 组合式函数
├── layouts/                # 布局组件
├── router/                 # 路由配置
├── stores/                 # Pinia 状态管理
├── styles/                 # 全局样式
├── types/                  # TypeScript 类型定义
├── utils/                  # 工具函数
├── views/                  # 页面视图
│   ├── LoginView.vue       # 登录页
│   ├── AuthCallbackView.vue # OAuth 回调页
│   ├── AuthConsentView.vue  # 授权同意页
│   ├── DashboardView.vue   # 仪表盘
│   ├── UsersView.vue       # 用户管理
│   ├── RolesView.vue       # 角色管理
│   ├── PermissionsView.vue # 权限管理
│   ├── DepartmentsView.vue # 部门管理
│   ├── TenantsView.vue     # 租户管理
│   ├── OAuthClientsView.vue # OAuth2 客户端
│   ├── OAuthScopesView.vue # OAuth2 作用域
│   ├── ConsentsView.vue    # 授权记录
│   ├── TenantCatalogView.vue # 租户目录
│   ├── AuditView.vue       # 审计日志
│   └── System*.vue         # 系统配置相关
└── main.ts                 # 应用入口
```

## 9. 常见问题

### Q1：前端一直跳转到登录页或提示 401
**可能原因**：
- 后端服务未启动在 8080 端口
- 浏览器访问地址与 allowed-origins 配置不一致
- token 已过期或被清除

**排查步骤**：
1. 确认后端已启动：`curl http://127.0.0.1:8080/actuator/health`
2. 检查浏览器访问地址是否为 `http://127.0.0.1:5173`
3. 清除浏览器 Cookie 后重新登录
4. 检查后端日志是否有认证相关错误

### Q2：登录成功但菜单空白
**原因**：当前账号没有分配菜单权限

**解决步骤**：
1. 使用 admin/123456 登录 platform 租户
2. 进入「角色管理」
3. 为目标角色分配权限（确保包含菜单的权限码）
4. 重新登录

### Q3：无法切换租户
**原因**：
- 只有平台超级管理员（platform 租户）可跨租户操作
- 普通租户用户会被限制在所属租户

**说明**：租户切换功能仅供平台级管理员使用，普通用户无需切换。

### Q4：OAuth2 客户端无法登录
**检查项**：
- 客户端状态是否为「启用」
- 重定向 URI 是否与实际请求一致
- 客户端密钥是否正确
- 是否授予了必要的 scope

### Q5：审计日志导出失败
**可能原因**：
- 导出任务正在处理中，请稍后再试
- 数据量过大，建议缩小时间范围
- 磁盘空间不足

**排查**：
1. 进入「导出任务」查看任务状态
2. 如状态为失败，点击「重试」
3. 检查后端日志

### Q6：数据库时间显示与实际不符
**原因**：系统统一按 UTC 存储时间

**解决方案**：
```sql
-- 查看时区设置
SHOW VARIABLES LIKE 'time_zone';

-- 临时切换为北京时间
SET time_zone = '+08:00';

-- 或使用脚本查看
source src/main/resources/database/timezone_check_and_view.sql
```

推荐在数据库客户端连接配置中设置会话初始化 SQL：
- Navicat：连接属性 → 高级 → 初始化命令
- DBeaver：连接设置 → Initialization → Initial SQL

### Q7：生产环境最容易遗漏的配置
- `issuer` 必须改为正式 HTTPS 域名
- `jwt-secret`、数据库密码、前端 client secret 必须替换
- 仅保留真实 `allowed-origins` 和回调地址白名单

**务必执行**：参照 `DEPLOYMENT_CHECKLIST.md` 逐项检查

## 10. 上线前检查清单

### 重要提醒
- 部署前请务必逐一核对以下检查项
- 详细清单见 `DEPLOYMENT_CHECKLIST.md`

### 重点检查项
- [ ] 安全密钥替换（jwt-secret、数据库密码）
- [ ] CORS 与回调地址白名单配置
- [ ] 租户头透传与防注入
- [ ] 认证/审计链路冒烟测试
- [ ] MySQL 和 Redis 连接配置正确
- [ ] issuer 使用正式 HTTPS 域名
- [ ] 应用程序日志无持续 ERROR

## 11. 代码质量检查

### 后端

```bash
# 编译
mvn "-Dmaven.repo.local=.m2repo" compile

# 测试
mvn "-Dmaven.repo.local=.m2repo" test

# 完整验证
mvn "-Dmaven.repo.local=.m2repo" verify
```

### 前端

```bash
cd frontend

# 代码检查
npm run lint

# 构建
npm run build

# E2E 测试
npm run test:e2e

# 视觉回归测试
npm run test:visual

# 更新视觉回归快照
npm run test:visual:update
```

## 12. 附录

### A. 技术栈版本

| 组件 | 版本 |
|------|------|
| Spring Boot | 3.2.5 |
| Spring Security | 6.x |
| Spring Authorization Server | 1.2.x |
| MyBatis-Plus | 3.5.x |
| MySQL | 8.0 |
| Redis | 6+ |
| Vue | 3.4+ |
| Vite | 5.x |
| Element Plus | 2.5.x |
| Pinia | 2.x |

### B. 相关文件路径

- 数据库初始化脚本：`src/main/resources/database/enterprise_auth_platform.sql`
- 时区查看脚本：`src/main/resources/database/timezone_check_and_view.sql`
- 后端配置文件：`src/main/resources/application.yml`
- 前端配置文件：`frontend/.env`、`frontend/.env.production`
- CI 工作流：`.github/workflows/ci.yml`

### C. 认证链路流程

```
/login（前端）→ /login（后端表单认证）→ /auth/consent（前端授权页）→ /auth/callback（前端换码）
```

### D. 联系方式

如有疑问或问题，请查阅项目 README.md 或联系系统管理员。
