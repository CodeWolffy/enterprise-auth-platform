# 企业级权限管理平台拓展计划

## 1. 计划目标

将现有认证权限基座扩展为可复用的企业级 IAM 底座，优先补齐生产安全闭环、账号自助、通知、文件、操作日志和运行总览能力，再逐步扩展工作流、代码生成、应用接入和策略化授权。

第一批交付目标：完成 **P0 安全闭环与通知**、**P1 底座实用能力**，让底座达到可直接复用到新项目的水平。

---

## 2. 总体路线

| 阶段 | 主题 | 目标 | 预计工期 | 交付优先级 |
|---|---|---|---|---|
| P0 | 安全闭环与通知 | 补齐账号、密码、会话、重置密码和邮件通知闭环 | 2-3 周 | 第一批 |
| P1 | 底座实用能力 | 补齐文件、头像、操作日志、仪表盘、通用导出 | 3-4 周 | 第一批 |
| P2 | 工作流与代码生成 | 增加审批流和 CRUD 生成能力 | PoC 1 周，MVP 3-4 周 | 后续 |
| P3 | 应用接入目录 | 建立应用、密钥、授权、门户能力 | 2-3 周 | 后续 |
| P4 | 策略化授权 | 在 RBAC 基础上增加条件授权、预演和影响分析 | 3-4 周 | 后续 |

第一批累计工期：**5-7 周**。

---

## 3. 全局约束

| 约束 | 要求 |
|---|---|
| 认证主链路 | 继续使用 Sa-Token Header Token |
| 浏览器令牌 | 不引入自建 access token / refresh token 双令牌模型 |
| 模块边界 | 新能力继续遵守 `interfaces -> application -> domain -> infrastructure` |
| 权限码 | 后端、前端、菜单初始化统一登记，避免散写 |
| 数据库迁移 | Flyway 只新增版本脚本，不修改历史基线 |
| 会话失效 | 复用现有 `sys_user.session_version`，不新增 `password_version` |
| 敏感数据 | 密码、密钥、重置令牌不存明文；密钥明文只展示一次 |
| 通知安全 | staging/prod 禁止将日志通知作为 SMTP fallback |
| 文件安全 | 文件访问不能只依赖 `fileKey`，必须结合租户、所有者、可见性和业务授权 |

---

## 4. P0：安全闭环与通知

### 4.1 目标

让登录、密码、会话、账号自助、忘记密码和通知具备生产可用的安全闭环。

### 4.2 子阶段

| 子阶段 | 状态 | 目标 | 核心交付 |
|---|---|---|---|
| P0-A | 已完成 | 安全策略基础 | 平台默认策略、租户覆盖策略、策略合并、密码规则参数化 |
| P0-B | 已完成 | 登录与会话闭环 | 强制改密、密码过期、受限改密态、旧会话失效 |
| P0-C | 已完成 | 重置密码与通知 | 重置 token、邮件通知、公开接口频控、防账号枚举、审计事件 |

### 4.2.1 P0-A 完成记录

| 项目 | 落地情况 |
|---|---|
| 数据迁移 | 新增 `V202606020002__security_policy_baseline.sql`，创建 `sys_platform_security_policy` 和 `sys_tenant_security_policy` |
| 策略模型 | 已支持平台默认值、租户覆盖值和空值继承合并 |
| 密码规则 | `PasswordValidator` 已改为读取当前租户生效策略 |
| 登录失败锁定 | `LoginAttemptService` 已改为读取租户策略中的失败阈值、锁定时长和统计窗口 |
| 租户初始化 | 新建租户会创建空覆盖策略，默认继承平台安全基线 |
| API | 已提供 `GET/PUT /api/security/policy` 和 `GET/PUT /api/security/policy/platform` |
| 权限与审计 | 已登记 `security:read`、`security:write`；策略变更写入 `PLATFORM_SECURITY_POLICY_UPDATED`、`TENANT_SECURITY_POLICY_UPDATED` |
| 验证 | `mvn -DskipTests compile` 通过 |

### 4.2.2 P0-B 完成记录

| 项目 | 落地情况 |
|---|---|
| 数据迁移 | 新增 `V202606020001__user_must_change_password.sql`，为 `sys_user` 增加 `must_change_password` |
| 登录响应 | `POST /api/auth/login` 返回 `passwordChangeRequired` 和 `passwordChangeReason`，用于前端识别受限改密态 |
| 密码过期 | 基于 `password_updated_at + 当前租户生效策略 password_expire_days` 动态判断，不落库 `password_expired` |
| 强制改密 | 新建用户和管理员重置密码后标记 `must_change_password=1`，用户登录后进入受限改密态 |
| 受限态边界 | 后端只放行 `POST /api/account/password/change`，其他受保护接口返回 `PASSWORD_CHANGE_REQUIRED` |
| 账号自助 | 新增 `GET /api/account/profile` 和 `POST /api/account/password/change`，登录即可访问，受限态下仅改密接口可用 |
| 会话失效 | 用户改密成功后自增 `session_version`，当前 token session 同步新版本，旧会话按版本不一致失效 |
| 前端入口 | 新增 `/account/profile`，受限态登录后直接进入改密页，正常态展示账号资料和改密入口 |
| 审计 | 用户主动改密记录 `PASSWORD_CHANGED`，密码过期登录记录 `PASSWORD_EXPIRED_BLOCKED` |
| 验证 | `mvn clean test -DskipTests` 与 `npm run build` 通过 |

### 4.2.3 P0-C 完成记录

| 项目 | 落地情况 |
|---|---|
| 数据迁移 | 新增 `V202606020003__password_reset_token.sql`，创建 `sys_password_reset_token`，仅保存 token 哈希 |
| 公开接口 | 新增 `POST /api/auth/password/reset/request`、`POST /api/auth/password/reset/verify`、`POST /api/auth/password/reset/confirm` |
| 防账号枚举 | 发起重置请求统一返回提示，不暴露账号是否存在或是否绑定邮箱 |
| 频控 | 公开请求使用 IP 频控，并按用户名与 IP 窗口统计重置请求次数 |
| 通知 | 支持 `log` 与 `smtp` 通道；staging/prod 要求 SMTP，禁止日志 fallback |
| 重置安全 | 重置成功后更新密码、清除强制改密标记、自增 `session_version` 并清理旧会话 |
| 审计 | 记录 `PASSWORD_RESET_REQUESTED`、`PASSWORD_RESET_COMPLETED`、`PASSWORD_RESET_FAILED` |
| 前端入口 | 新增 `/reset-password`，登录页提供“忘记密码”入口，支持申请和确认重置两段流程 |
| 验证 | `mvn clean test -DskipTests` 与 `npm run build` 通过 |

### 4.3 范围

- 租户级安全策略：密码复杂度、密码历史、密码过期、登录失败锁定、验证码开关。
- 登录失败锁定参数化：`LoginAttemptService` 从硬编码改为读取安全策略。
- 密码校验参数化：`PasswordValidator` 从固定正则改为读取安全策略。
- 强制改密：`sys_user` 新增 `must_change_password`。
- 密码过期：基于 `password_updated_at + password_expire_days` 动态判断，不落库 `password_expired`。
- 密码修改、密码重置、管理员重置密码后自增 `session_version`。
- 忘记密码：请求、校验、确认重置完整流程。
- 邮件通知：dev/local 使用日志通道，staging/prod 使用 SMTP。
- 账号自助：我的资料、修改密码。
- 安全审计事件补齐。

### 4.4 不做

- 不改为 Cookie Session 主链路。
- 不接入短信供应商。
- 不新增 `password_version`。
- 不把账号自助类接口强制绑定权限码，登录即可访问。

### 4.5 数据规划

| 表/字段 | 用途 |
|---|---|
| `sys_platform_security_policy` | 平台默认安全策略 |
| `sys_tenant_security_policy` | 租户安全策略覆盖，空值继承平台默认值 |
| `sys_password_history` | 密码历史记录 |
| `sys_password_reset_token` | 重置密码令牌生命周期 |
| `sys_user.must_change_password` | 强制改密标记 |

### 4.6 API 规划

| 接口 | 说明 | 权限 |
|---|---|---|
| `GET /api/security/policy` | 查询当前租户生效安全策略 | `security:read` |
| `PUT /api/security/policy` | 更新当前租户安全策略覆盖 | `security:write` |
| `GET /api/security/policy/platform` | 查询平台默认安全策略 | 平台超级管理员 + `security:read` |
| `PUT /api/security/policy/platform` | 更新平台默认安全策略 | 平台超级管理员 + `security:write` |
| `POST /api/security/unlock-account` | 管理员手动解锁账号 | `security:write` |
| `GET /api/account/profile` | 我的资料 | 登录即可 |
| `PUT /api/account/profile` | 更新我的资料 | 登录即可 |
| `POST /api/account/password/change` | 修改自己的密码 | 登录即可；受限改密态可访问 |
| `POST /api/auth/password/reset/request` | 发起忘记密码 | 公开接口 + 频控 |
| `POST /api/auth/password/reset/verify` | 校验重置令牌 | 公开接口 |
| `POST /api/auth/password/reset/confirm` | 确认重置密码 | 公开接口 |

### 4.7 前端入口

| 页面 | 路径 | 权限 |
|---|---|---|
| 安全策略中心 | `/system/security-policy` | `security:read` |
| 我的账号 | `/account/profile` | 登录即可 |
| 重置密码 | `/reset-password` | 公开路由 |

### 4.8 权限码

| 权限码 | 用途 |
|---|---|
| `security:read` | 查看安全策略 |
| `security:write` | 修改安全策略、解锁账号 |

### 4.9 审计事件

| 事件 | 触发时机 |
|---|---|
| `PLATFORM_SECURITY_POLICY_UPDATED` | 平台安全策略变更 |
| `TENANT_SECURITY_POLICY_UPDATED` | 租户安全策略变更 |
| `PASSWORD_CHANGED` | 用户主动改密 |
| `PASSWORD_RESET_REQUESTED` | 发起忘记密码 |
| `PASSWORD_RESET_COMPLETED` | 重置密码成功 |
| `PASSWORD_RESET_FAILED` | 重置令牌无效、过期、已使用或已废弃 |
| `PASSWORD_EXPIRED_BLOCKED` | 密码过期阻断登录 |
| `ACCOUNT_LOCKED` | 登录失败达到锁定阈值 |
| `LOGIN_BLOCKED` | 被锁账号再次尝试登录 |
| `FORCE_UNLOCK` | 管理员手动解锁账号 |

### 4.10 验收点

- 安全策略支持平台默认值和租户覆盖值合并。
- 新建租户自动继承平台安全基线。
- 密码复杂度、密码历史、密码过期按租户策略生效。
- 登录失败锁定阈值和锁定时长按租户策略生效。
- 强制改密或密码过期用户只能进入受限改密态。
- 受限改密态只能访问改密页和改密接口。
- 重置密码接口防账号枚举，统一返回提示。
- 重置密码请求按用户名和 IP 频控。
- 重置密码成功后旧会话失效。
- SMTP 失败在 staging/prod 失败关闭，不把重置链接写入日志。
- 安全策略、密码、锁定、重置相关事件可审计。

---

## 5. P1：底座实用能力

### 5.1 目标

补齐复用底座在日常企业后台中的基础能力。

### 5.1.1 当前状态

| 子阶段 | 状态 | 完成时间 | 完成范围 |
|---|---|---|---|
| P1-A | 已完成 | 2026-06-04 | 账号头像链路、文件管理前端、仪表盘统计、操作日志正式入口与 CSV 导出防护 |
| P1 收口 | 已完成 | 2026-06-04 | 文件私有边界、操作日志脱敏、专项测试、配置切换验收 |

边界说明：

- P1-A 已完成，不等同于 P1 整体完成。
- 操作日志目前复用 `sys_audit_log` 作为统一日志载体，正式入口使用 `/api/operation-logs` 和 `/system/operation-logs`。
- 本次未做“全接口自动记录”，操作日志仍按关键业务动作采集。
- 本次未强制交付 XLSX 导出，操作日志正式入口优先交付 CSV 导出。
- 通用文件上传不开放 `PRIVATE` 可见性，私有文件必须通过具体业务授权链路创建。
- 文件存储基础来自 `V202606030004__file_storage_minio.sql`，P1-A 菜单、权限和操作日志入口来自 `V202606040001__p1a_dashboard_operation_logs.sql`。

### 5.2 范围

- 用户头像上传与裁剪。
- 统一文件上传、存储、访问控制。
- 本地存储和 MinIO/S3 存储切换。
- 操作日志查询和导出。
- 仪表盘统计数据。
- 通用数据导出抽象，优先交付 CSV。

### 5.3 不做

- 不引入 WebAssembly 前端框架。
- 不默认记录所有查询接口为操作日志。
- 不在 P1 强制交付 XLSX 导出。

### 5.4 数据规划

| 表/字段 | 用途 |
|---|---|
| `sys_storage_file` | 文件存储记录 |
| `sys_audit_log` | 操作日志和审计事件统一载体，`/api/operation-logs` 为正式操作日志入口 |
| `sys_user.avatar_file_key` | 用户头像文件关联 |

### 5.5 API 规划

| 接口 | 说明 | 权限 |
|---|---|---|
| `POST /api/files/upload` | 上传文件 | `file:write` |
| `GET /api/files` | 文件分页查询 | `file:read` |
| `GET /api/files/{fileKey}/metadata` | 获取文件元数据 | 按文件访问规则判断 |
| `GET /api/files/{fileKey}` | 获取文件 | 按文件访问规则判断 |
| `GET /api/files/public/{fileKey}` | 匿名读取公开文件 | 公开文件 |
| `DELETE /api/files/{fileKey}` | 删除文件 | `file:write` |
| `PUT /api/account/avatar` | 上传或更新我的头像 | 登录即可 |
| `PUT /api/account/profile/avatar` | 上传或更新我的头像，兼容入口 | 登录即可 |
| `GET /api/dashboard/stats` | 仪表盘统计 | `dashboard:read` |
| `GET /api/operation-logs` | 操作日志查询 | `operation-log:read` |
| `GET /api/operation-logs/export` | 操作日志导出 | `operation-log:export` |

### 5.6 文件访问规则

| 可见性 | 访问边界 |
|---|---|
| `PUBLIC` | 头像、应用图标等公开文件 |
| `TENANT` | 同租户登录用户可访问 |
| `OWNER` | 上传者或所有者可访问 |
| `PRIVATE` | 通用文件上传不开放；必须通过具体业务授权链路创建和访问 |

### 5.7 操作日志采集规则

| 方式 | 场景 |
|---|---|
| 注解式采集 | 用户、角色、资源、租户等标准 CRUD |
| 显式发布事件 | 授权、审批、密钥轮换等复杂动作 |
| 全接口自动记录 | 不采用，避免噪声和敏感数据泄露 |

### 5.8 仪表盘统计范围

| 用户类型 | 统计范围 |
|---|---|
| 平台超级管理员 | 全平台统计 |
| 租户管理员 | 当前租户统计 |
| 普通用户 | 可见范围内的基础统计 |

### 5.9 前端入口

| 页面 | 路径 | 权限 |
|---|---|---|
| 运行总览 | `/dashboard` | `dashboard:read` |
| 文件管理 | `/platform/files` | `file:read` |
| 操作日志 | `/system/operation-logs` | `operation-log:read` |
| 我的账号头像 | `/account/profile` | 登录即可 |

### 5.10 权限码

| 权限码 | 用途 |
|---|---|
| `file:read` | 文件查看 |
| `file:write` | 文件管理 |
| `dashboard:read` | 仪表盘查看 |
| `operation-log:read` | 操作日志查看 |
| `operation-log:export` | 操作日志导出 |

### 5.11 审计事件

| 事件 | 触发时机 |
|---|---|
| `FILE_UPLOADED` | 文件上传成功 |
| `FILE_DELETED` | 文件删除 |
| `AVATAR_UPDATED` | 用户更新头像 |

### 5.12 P1-A MinIO 文件能力基础建设完成记录

| 项目 | 落地情况 |
|---|---|
| 数据迁移 | 新增 `V202606030004__file_storage_minio.sql`，创建 `sys_storage_file`，并为 `sys_user` 增加 `avatar_file_key` |
| 存储配置 | 新增 `platform.file` 与 `platform.minio` 配置模板，使用环境变量占位；启动阶段不强连 MinIO |
| 存储适配 | 已接入 MinIO，对象桶在上传时按需创建；上传异常对接口隐藏底层存储细节 |
| 文件接口 | 已提供文件分页、上传、元数据查询、登录态下载、匿名公开下载和删除能力 |
| 访问控制 | 默认上传为 `OWNER`；`PUBLIC` 创建需 `file:write`；通用上传拒绝 `PRIVATE`，私有文件只保留给具体业务授权链路；public 路径只读取公开文件 |
| 权限与白名单 | 已登记 `file:read`、`file:write`，并放行 `/api/files/public/**` |
| 验证 | `mvn -DskipTests compile` 通过 |

### 5.13 P1-A 完成记录

| 项目 | 落地情况 |
|---|---|
| 账号头像链路 | 已提供 `/api/account/avatar` 和兼容入口 `/api/account/profile/avatar`；账号资料、权限快照和前端顶部用户信息已透传头像 |
| 文件管理前端 | 已新增 `/platform/files`，支持分页查询、上传、下载、删除和公开链接复制；按 `file:read`、`file:write` 控制入口与操作 |
| 仪表盘统计 | 已新增 `GET /api/dashboard/stats`，运行总览页已接入真实统计数据；使用 `dashboard:read` 权限 |
| 操作日志正式入口 | 已新增 `/api/operation-logs`，原 `/api/audit` 保留兼容；前端正式入口为 `/system/operation-logs` |
| 操作日志导出 | 已提供 CSV 导出，使用 `operation-log:export` 权限，并做导出频控、导出上限和 CSV 公式注入防护 |
| 菜单与权限 | `V202606040001__p1a_dashboard_operation_logs.sql` 已登记 dashboard、operation-log 菜单/API 资源和平台管理员授权；`audit:*` 保留兼容，`operation-log:*` 作为正式权限 |
| 验证 | `mvn -DskipTests compile`、`npm run build` 通过；本地 Flyway 迁移 `202606040001` 已修复并成功执行 |

### 5.13.1 P1 收口完成记录

| 项目 | 落地情况 |
|---|---|
| 文件私有边界 | 通用文件上传已拒绝 `PRIVATE` 可见性，避免绕过具体业务授权链路；前端上传可见性不再提供私有选项 |
| 存储配置切换 | 已补本地、MinIO 和 S3 alias 路由测试；按 bucket 回读和删除路由已覆盖，不依赖真实 MinIO |
| 操作日志脱敏 | 新增中心化操作日志明细脱敏，密码、token、secret、credential、authorization、reset link 等敏感字段入库、查询出口和导出前统一掩码 |
| 导出防护 | 操作日志 CSV 导出已覆盖敏感字段脱敏和公式注入防护 |
| 仪表盘隔离 | 已覆盖租户级统计隔离和普通用户可见范围隔离 |
| 专项测试 | 已新增文件 PRIVATE 边界、存储配置切换、仪表盘隔离、操作日志查询脱敏、CSV 导出脱敏与公式防护测试 |
| 验证 | `mvn clean "-Dtest=RoutingObjectStorageServiceTest,DashboardStatsServiceP1Test,OperationLogCsvExportServiceP1Test,AuditServiceQueryTest,FileControllerP1Test" test`、`npm run build` 通过 |

### 5.14 验收点

- 用户可以在“我的账号”页上传和裁剪头像。
- 文件上传支持本地存储和 MinIO/S3，通过配置切换。
- 文件下载按可见性、租户、所有者和业务授权判断；通用上传不开放 `PRIVATE`。
- 上传文件做类型白名单、大小限制和真实格式校验。
- 仪表盘按用户身份隔离统计范围。
- 操作日志记录关键业务变更，支持分页查询和导出。
- CSV 导出具备公式注入防护。
- 导出字段按白名单输出，敏感字段默认脱敏。
- 本地存储、MinIO 和 S3 alias 配置切换已通过路由专项测试覆盖。
- 仪表盘统计隔离和操作日志导出防护已通过专项自动化测试覆盖。

---

## 6. P2：工作流与代码生成

### 6.1 目标

增加流程审批能力和开发提效能力。

### 6.2 前置条件

P2 启动前先完成工作流技术选型和最小 PoC。PoC 必须验证：顺序审批、驳回、撤回、候选人/候选组、流程变量快照、租户隔离和权限隔离。

### 6.3 范围

| 能力 | 交付 |
|---|---|
| 工作流定义 | 流程定义、版本、部署、停用 |
| 流程实例 | 发起、撤回、终止、完成 |
| 审批任务 | 待办、已办、审批、驳回、转签、催办 |
| 审批人分配 | 按角色、部门、岗位、候选人、候选组分配 |
| 流程变量 | 发起时绑定表单数据，审批时可查看快照 |
| 代码生成 | 选表、预览、生成后端和前端 CRUD 代码 |
| 模板管理 | 模板可自定义，生成结果可预览、可覆盖 |

### 6.4 数据规划

| 表 | 用途 |
|---|---|
| `wf_process_definition` | 流程定义 |
| `wf_process_instance` | 流程实例 |
| `wf_task` | 审批任务 |

### 6.5 API 分组

| 分组 | 路径前缀 | 权限 |
|---|---|---|
| 流程定义 | `/api/workflow/process-definitions` | `workflow:read/write` |
| 流程实例 | `/api/workflow/instances` | 登录即可，管理动作需 `workflow:write` |
| 我的任务 | `/api/workflow/tasks` | 登录即可 |
| 代码生成 | `/api/codegen` | `codegen:read/write` |

### 6.6 前端入口

| 页面 | 路径 | 权限 |
|---|---|---|
| 流程设计器 | `/platform/workflow/designer` | `workflow:write` |
| 流程定义 | `/platform/workflow/definitions` | `workflow:read` |
| 我的发起 | `/platform/workflow/my-instances` | 登录即可 |
| 我的待办 | `/platform/workflow/todo` | 登录即可 |
| 我的已办 | `/platform/workflow/done` | 登录即可 |
| 代码生成 | `/platform/codegen` | `codegen:read` |

### 6.7 权限码

| 权限码 | 用途 |
|---|---|
| `workflow:read` | 工作流查看 |
| `workflow:write` | 工作流管理 |
| `codegen:read` | 代码生成查看 |
| `codegen:write` | 代码生成管理 |

### 6.8 验收点

- PoC 通过后再进入 MVP 开发。
- 流程定义可部署、停用，历史版本可追溯。
- 发起人可查看自己的流程实例。
- 审批人可处理待办任务。
- 审批动作记录操作日志和审计事件。
- 流程变量快照不可被审批过程篡改。
- 代码生成结果可预览、可选择覆盖范围。

### 6.9 工作流 PoC 完成记录

| 项目 | 落地情况 |
|---|---|
| 技术路线 | 先采用项目内置轻量状态机，不直接引入 Flowable/Activiti；状态转换集中在 `workflow` 模块，后续可替换外部流程引擎 |
| 数据迁移 | 新增 `V202606040002__p2_workflow_poc.sql`，创建 `wf_process_definition`、`wf_process_instance`、`wf_task` 三张工作流表 |
| 后端接口 | 已提供流程定义创建/查询/部署/停用，流程实例发起/我的发起/撤回/终止，待办/已办/审批/驳回接口 |
| 权限与菜单 | 已登记 `workflow:read`、`workflow:write`、`codegen:read`、`codegen:write`；工作流菜单挂在平台管理下 |
| PoC 验证项 | 已覆盖顺序审批、驳回、撤回、候选人/候选组、变量快照、租户隔离和权限隔离 |
| 前端入口 | 已新增流程定义、我的发起、我的待办、我的已办四个最小可用页面，接入后端真实 API |
| 验证 | `mvn -Dtest=WorkflowApplicationServiceP2Test test`、`npm run build` 通过 |
| 未完成范围 | 流程设计器、转签、催办仍属于 P2 后续工作流 MVP 范围 |

### 6.10 代码生成 MVP 完成记录

| 项目 | 落地情况 |
|---|---|
| 技术路线 | 先做项目内置代码生成 MVP，不引入模板引擎；基于 MySQL `information_schema` 读取表和字段元数据 |
| 后端接口 | 已提供数据表分页、表字段详情、生成预览、生成到隔离目录接口 |
| 生成范围 | 支持后端实体、Mapper、应用服务、控制器，以及前端类型、API 包装、最小列表页预览 |
| 安全边界 | 生成结果默认写入 `target/generated-codegen`；默认拒绝覆盖已有文件；校验表名、模块名、类名、包名和输出路径，避免路径越界 |
| 权限与菜单 | 代码生成菜单已挂在平台管理下，使用 `codegen:read`；生成落盘动作使用 `codegen:write` |
| 前端入口 | 已新增 `/platform/codegen`，支持选表、查看字段、配置模块、预览文件和生成到隔离目录 |
| 验证 | `mvn -Dtest=CodegenApplicationServiceP2Test,WorkflowApplicationServiceP2Test test`、`npm run build` 通过 |
| 未完成范围 | 模板自定义、覆盖范围选择、生成产物导出/下载、生成后自动注册菜单和权限仍属后续增强 |

---

## 7. P3：应用接入目录

### 7.1 目标

建立应用、接入密钥、授权、门户模型，为后续 SSO 和应用级授权打底。

### 7.2 范围

- 应用管理。
- 应用密钥创建、轮换、禁用。
- 应用授权：租户、角色、用户维度。
- 应用门户：展示当前用户可访问应用。
- 应用访问审计。

### 7.3 不做

- 不给外部应用直接下发长期用户凭证。
- 不让应用门户承载内部管理后台菜单。
- 不把应用门户放入管理后台侧边栏，使用独立入口 `/app-portal`。

### 7.4 数据规划

| 表 | 用途 |
|---|---|
| `sys_app` | 应用目录 |
| `sys_app_secret` | 应用密钥 |
| `sys_app_assignment` | 应用授权 |
| `sys_app_access_log` | 应用访问记录 |

### 7.5 API 分组

| 分组 | 路径前缀 | 权限 |
|---|---|---|
| 应用管理 | `/api/apps` | `app:read/write` |
| 应用门户 | `/api/app-portal/apps` | 登录即可 |

### 7.6 前端入口

| 页面 | 路径 | 权限 |
|---|---|---|
| 应用管理 | `/platform/apps` | `app:read` |
| 应用密钥 | `/platform/apps/:id/secrets` | `app:write` |
| 应用授权 | `/platform/apps/:id/assignments` | `app:write` |
| 应用门户 | `/app-portal` | 登录即可 |

### 7.7 权限码

| 权限码 | 用途 |
|---|---|
| `app:read` | 应用查看 |
| `app:write` | 应用管理、密钥管理、授权管理 |

### 7.8 授权决策规则

| 场景 | 规则 |
|---|---|
| 存在显式拒绝 | 拒绝优先 |
| 用户级授权 | 优先于角色级授权 |
| 角色级授权 | 优先于租户级授权 |
| 没有任何授权 | 默认拒绝 |

### 7.9 验收点

- 应用密钥明文只在创建或轮换时展示一次。
- 禁用应用后，应用门户不再展示。
- 用户只能看到授权范围内的应用。
- 应用访问决策可解释，允许或拒绝原因进入审计。

---

## 8. P4：策略化授权

### 8.1 目标

保留当前 RBAC + 数据范围模型，增加条件授权、策略预演和影响分析。

### 8.2 范围

- 角色资源授权附加条件：时间窗口、IP 段、部门、资源属性。
- 策略预演：输入用户、租户、资源、环境上下文，输出允许或拒绝原因。
- 影响分析：展示策略变更前后影响的用户、角色、菜单或接口。
- 授权判断命中路径展示。

### 8.3 不做

- 不直接引入 OPA/Casbin 作为第一版策略引擎。
- 不允许任意表达式、脚本或动态 SQL。
- 不让策略管理权限并入角色管理权限。

### 8.4 数据规划

| 表 | 用途 |
|---|---|
| `sys_auth_policy` | 策略定义 |
| `sys_role_resource_policy` | 角色资源策略关联 |
| `sys_authz_decision_log` | 授权决策日志 |

### 8.5 API 分组

| 分组 | 路径前缀 | 权限 |
|---|---|---|
| 授权策略 | `/api/authz/policies` | `policy:read/write` |
| 策略预演 | `/api/authz/policies/simulate` | `policy:read` |
| 影响分析 | `/api/authz/policies/impact` | `policy:read` |

### 8.6 权限码

| 权限码 | 用途 |
|---|---|
| `policy:read` | 授权策略查看、预演、影响分析 |
| `policy:write` | 授权策略管理 |

### 8.7 验收点

- 旧角色授权无需迁移即可继续使用。
- 新策略只在显式配置后生效。
- 条件解析失败、字段不在白名单、操作符不支持时默认拒绝。
- 策略预演和真实授权使用同一套评估逻辑。
- 管理员能看到授权判断的命中路径。

---

## 9. 第一批实施顺序

| 顺序 | 阶段 | 交付内容 | 检查点 |
|---|---|---|---|
| 1 | P0-A | 安全策略表、平台默认值、租户覆盖、策略合并 | 能查询当前租户生效策略 |
| 2 | P0-A | 密码校验参数化、登录锁定参数化 | 不再依赖硬编码阈值和正则 |
| 3 | P0-B | `must_change_password`、密码过期判断、受限改密态 | 强制改密用户不能进入后台业务页 |
| 4 | P0-B | 改密后自增 `session_version` | 旧会话失效 |
| 5 | P0-C | 重置密码 token、频控、防枚举 | 公开接口行为稳定 |
| 6 | P0-C | 邮件通知、SMTP 配置、通知失败策略 | staging/prod 不泄露 token |
| 7 | P1-A | 文件存储表、存储抽象、本地存储 | 文件可上传和读取 |
| 8 | P1-A | MinIO/S3 适配、文件访问控制 | 按可见性和授权访问 |
| 9 | P1-A | 头像上传与裁剪 | 我的账号头像可更新 |
| 10 | P1-B | 操作日志采集、查询、导出 | 关键业务变更可追溯 |
| 11 | P1-C | 仪表盘统计 | 按用户身份隔离统计范围 |
| 12 | P1 收口 | 权限码、菜单、审计事件、基础测试 | 第一批可验收 |

---

## 10. 模块归属

| 能力 | 后端模块 | API 前缀 |
|---|---|---|
| 安全策略 | `modules/security` | `/api/security` |
| 账号自助 | `modules/account` | `/api/account` |
| 重置密码 | `modules/auth` | `/api/auth/password/reset` |
| 文件管理 | `modules/file` | `/api/files` |
| 仪表盘 | `modules/dashboard` | `/api/dashboard` |
| 操作日志 | `modules/operationlog` | `/api/operation-logs` |
| 应用接入 | `modules/app` | `/api/apps`、`/api/app-portal` |
| 策略授权 | `modules/authz` | `/api/authz/policies` |

---

## 11. 全局权限码规划

| 权限码 | 阶段 | 用途 |
|---|---|---|
| `security:read` | P0 | 安全策略查看 |
| `security:write` | P0 | 安全策略管理 |
| `file:read` | P1 | 文件查看 |
| `file:write` | P1 | 文件管理 |
| `dashboard:read` | P1 | 仪表盘查看 |
| `operation-log:read` | P1 | 操作日志查看 |
| `operation-log:export` | P1 | 操作日志导出 |
| `workflow:read` | P2 | 工作流查看 |
| `workflow:write` | P2 | 工作流管理 |
| `codegen:read` | P2 | 代码生成查看 |
| `codegen:write` | P2 | 代码生成管理 |
| `app:read` | P3 | 应用查看 |
| `app:write` | P3 | 应用管理 |
| `policy:read` | P4 | 授权策略查看 |
| `policy:write` | P4 | 授权策略管理 |

---

## 12. 全局数据规划

| 阶段 | 数据项 |
|---|---|
| P0 | `sys_platform_security_policy`、`sys_tenant_security_policy`、`sys_password_history`、`sys_password_reset_token`、`sys_user.must_change_password` |
| P1 | `sys_storage_file`、`sys_operation_log`、`sys_user.avatar_file_key` |
| P2 | `wf_process_definition`、`wf_process_instance`、`wf_task` |
| P3 | `sys_app`、`sys_app_secret`、`sys_app_assignment`、`sys_app_access_log` |
| P4 | `sys_auth_policy`、`sys_role_resource_policy`、`sys_authz_decision_log` |

---

## 13. 全局前端入口规划

| 阶段 | 页面 | 路径 |
|---|---|---|
| P0 | 安全策略中心 | `/system/security-policy` |
| P0 | 我的账号 | `/account/profile` |
| P0 | 重置密码 | `/reset-password` |
| P1 | 运行总览 | `/dashboard` |
| P1 | 文件管理 | `/platform/files` |
| P1 | 操作日志 | `/system/operation-logs` |
| P2 | 流程设计器 | `/platform/workflow/designer` |
| P2 | 流程定义 | `/platform/workflow/definitions` |
| P2 | 我的发起 | `/platform/workflow/my-instances` |
| P2 | 我的待办 | `/platform/workflow/todo` |
| P2 | 我的已办 | `/platform/workflow/done` |
| P2 | 代码生成 | `/platform/codegen` |
| P3 | 应用管理 | `/platform/apps` |
| P3 | 应用门户 | `/app-portal` |
| P4 | 授权策略 | `/system/auth-policies` |

---

## 14. 阶段依赖

| 依赖 | 原因 |
|---|---|
| P1 依赖 P0 | 文件、操作日志、仪表盘都依赖稳定登录态、租户上下文和审计基础 |
| P2 依赖 P0 + P1 | 工作流依赖用户、角色、部门、文件和操作记录能力 |
| P3 依赖 P1 | 应用图标依赖文件能力，应用变更依赖操作日志 |
| P4 依赖 P3 | 应用维度授权需要先有应用模型 |

可并行项：

- P2 中代码生成器可与工作流 MVP 并行。
- 数据脱敏、审计增强可在 P0 后并行。
- 短信、Webhook、i18n 可按业务需求单独启动。

---

## 15. 第一批不包含

| 能力 | 后续阶段 | 原因 |
|---|---|---|
| 工作流 | P2 | 需要单独 PoC 和状态机设计 |
| 代码生成器 | P2 | 属于开发提效能力，不阻塞底座生产可用 |
| 应用接入目录 | P3 | 依赖文件、操作日志等 P1 基础能力 |
| 策略化授权 | P4 | 依赖应用模型和授权决策日志稳定 |
| 数据脱敏 | 后置能力池 | 安全增强，不阻塞第一批闭环 |
| 短信通知 | 后置能力池 | P0 先用邮件完成重置密码闭环 |
| Webhook | 后置能力池 | 应用事件稳定后再开放 |
| i18n | 后置能力池 | 非底座前置条件 |

---

## 16. 第一批验收标准

- P0 + P1 范围内的每个能力都有数据表、接口、前端入口、权限码、审计事件。
- 所有新增能力复用当前 Sa-Token Session、租户上下文、权限快照。
- 新模块不破坏现有 `auth`、`user`、`role`、`resource`、`tenant`、`audit`、`system` 边界。
- 安全策略、密码重置、受限改密、旧会话失效可端到端验证。
- 文件上传、头像、操作日志、仪表盘、导出可端到端验证。
- 敏感字段不入日志、不入审计明文、不在接口重复展示。
- 第一批不依赖 SSO、短信、Webhook、策略化授权即可闭环。