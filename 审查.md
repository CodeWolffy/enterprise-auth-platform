# 代码审查报告 — 功能拓展分支

> 仓库:`enterprise-auth-platform`
> 审查分支:`功能拓展`(HEAD `a7a5f491dc997a2f367514318ce78c81008453a3`)
> 基准分支:`main`
> 提交范围:`main..HEAD`,11 个 commit
> 变更规模:202 个文件,+17,699 / -1,297
> 审查日期:2026-06-05
> HEAD SHA:`a7a5f491dc997a2f367514318ce78c81008453a3`
> GitHub 链接前缀:`https://github.com/CodeWolffy/enterprise-auth-platform/blob/a7a5f491dc997a2f367514318ce78c81008453a3/`

---

## 1. 范围与方法

- **范围**:`功能拓展` 分支领先 `main` 11 个 commit,202 个文件 / +17,699 / -1,297。
- **方法**:按 4 个领域拆分并行评审(认证安全 / 文件存储+邮件 / 工作流+代码生成 / 仪表盘+前端),交叉对照仓库内 `拓展计划.md`(等同项目级 CLAUDE.md)做计划合规过滤,只保留置信度 ≥ 80 的问题。
- **置信度门槛**:≥ 80。低于该门槛的发现列在 §5 观察和 §6 审查未覆盖中。
- **未运行**:`mvn` / `npm run build` / `mvn test` 等构建与测试,本评审不重复 CI 流程。

---

## 2. 安全与计划合规(10 条)

### A1. 密码重置接口可枚举账号 [置信度 95]
- **违反**:`拓展计划.md` §4.2.3 / §4.10 验收点"重置密码接口防账号枚举,统一返回提示"。
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/auth/application/PasswordResetApplicationService.java:88-129`
- **证据**:显式返回 `NOT_FOUND` / `EMAIL_NOT_CONFIGURED` / `EMAIL_SENT` 三种 result 码,以及"用户名不存在" / "该账号未绑定邮箱"两种文案,均能区分账号存在、已启用、是否绑邮箱。
- **建议**:三处失败路径统一返回 `GENERIC_REQUEST_MESSAGE` + `EMAIL_SENT`,仅审计日志保留真实原因。

### A2. SMTP 密码在缺 `APP_MAIL_SECRET_KEY` 时以明文入库 [置信度 95]
- **违反**:`拓展计划.md` §3 全局约束"敏感数据:密码、密钥、重置令牌不存明文"。
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/system/application/MailChannelSecretService.java:32-37`
- **证据**:
  ```java
  public String protect(String rawSecret) {
      if (!StringUtils.hasText(rawSecret) || isProtected(rawSecret) || !canProtect()) {
          return rawSecret;   // 缺密钥时直接返回原文
      }
  ```
  配合 `V202606030001__mail_channel.sql` 的 `mail_password VARCHAR(1024)`,运维一旦漏设 `APP_MAIL_SECRET_KEY`,SMTP 授权码将以明文落库,且 `isProtected()==false`,运行时无法察觉。
- **建议**:`!canProtect()` 时拒绝保存,启动期校验,prod 强制密文。

### A3. Workflow 写操作端点缺 `workflow:write` 权限 [置信度 90]
- **违反**:`拓展计划.md` §6.5"流程实例:登录即可,管理动作需 `workflow:write`"。
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/workflow/interfaces/WorkflowController.java`(start / withdraw / terminate 等端点)
- **证据**:发起流程、撤回、终止等"管理动作"端点无 `@SaCheckPermission(workflow:write)`;`/tasks/{id}/approve|reject|transfer|urge` 也未做 per-task assignee 校验的 controller 注释。
- **说明**:`/tasks/todo` / `/tasks/done` / `/instances/my` 计划明文"登录即可",服务层应做 assignee 校验,这些不算问题。
- **建议**:start / withdraw / terminate 三个端点补 `workflow:write`;审批/转签/催办在 service 层加 `assigneeUserId == currentUser.id()` 断言。

### A4. `WfTaskEntity` 缺 `@Version`,并发审批会丢失更新 [置信度 90]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/workflow/infrastructure/entity/WfTaskEntity.java`(全文件,40 行)
- **证据**:实体仅含 `id/tenantId/instanceId/stepIndex/status/...`,无 `@Version`;`WorkflowApplicationService` 中 `updateById(task)` 与 `requirePendingTask` 均无乐观锁,`UPDATE wf_task SET status=... WHERE id=?` 不带 `WHERE status='PENDING'`。
- **影响**:两人同时审批同一 PENDING 任务,后提交覆盖前者,审计事件重复、状态错乱。
- **建议**:加 `@Version` 或 SQL 加 `WHERE id=? AND status='PENDING'`,按 affected rows 判定冲突并抛 409。

### A5. Codegen `tables/preview/download` 缺表级白名单 [置信度 90]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/codegen/interfaces/CodegenController.java:38-83` + `CodegenApplicationService.java:65-125`
- **证据**:`tables` 走 `information_schema` 无表白名单,`preview` 接受任意 `tableName` 反射读 schema,`download` 导出整表结构 zip。
- **影响**:任何持 `codegen:read` 的账号可枚举全库表名/字段中文注释/列结构,业务表元数据泄露。
- **建议**:加 `sys_codegen_allowlist`,`download` 拆出独立权限 `codegen:download`。

### A6. `MailChannelController.testSend` 无限流 [置信度 90]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/system/interfaces/MailChannelController.java:78-89`
- **证据**:`/test` 端点仅 `@SaCheckPermission(SYSTEM_WRITE)`,无 `@RateLimiter`,`toEmail` 接收任意邮箱。
- **影响**:持 `system:write` 的租户管理员可批量对任意地址发任意主题,易被列入反垃圾黑名单 / 被用作开放 relay。
- **建议**:加 IP / 用户级 Bucket4j 限流,`toEmail` 做租户/域名白名单。

### A7. `DashboardStatsService.sumStorageBytes` 全表加载再求和 [置信度 90]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/dashboard/application/DashboardStatsService.java:127-134`
- **证据**:
  ```java
  return sysStorageFileMapper.selectList(wrapper).stream()
          .map(SysStorageFileEntity::getFileSize)
          .filter(size -> size != null && size > 0)
          .mapToLong(Long::longValue).sum();
  ```
- **影响**:当前租户所有 `sys_storage_file` 一次 SELECT 到 JVM,十万级文件数会 OOM + DB 拉满;同 service 内 `stats()` 还串行触发 30+ 次 COUNT/SELECT。
- **建议**:改为 `@Select("SELECT IFNULL(SUM(file_size),0) FROM ...")`;`stats()` 整体加 `@Cacheable(30s)` 或合并为单次 GROUP BY 聚合 SQL。

### A8. MinioProperties 默认 `minioadmin/minioadmin` + autoCreateBucket [置信度 85]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/file/infrastructure/MinioProperties.java:17-30` + `application.yml:107-114`
- **证据**:`resolvedEndpoint()="http://localhost:9000"`、`resolvedAccessKey()="minioadmin"`、`resolvedBucket()="enterprise-auth-platform"`、`resolvedAutoCreateBucket()=true` 全部硬编码默认。
- **影响**:运维漏配 `MINIO_*` 环境变量时 prod 也会连 `localhost:9000` + `minioadmin/minioadmin`,且自动建桶,跨环境对象污染。
- **建议**:环境变量未设时启动失败;`prod` / `staging` profile 强制 HTTPS + 非默认凭据。

### A9. `FileApplicationService.upload` 整文件加载到堆 [置信度 85]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/file/application/FileApplicationService.java:222-227, 178-185`
- **证据**:`file.getBytes()` 一次性拉到 `byte[]`,再 `new ByteArrayInputStream(...)` 喂 MinIO;`spring.servlet.multipart.max-file-size` 与业务 `FileStorageProperties.maxSize` 未对齐(后者默认 10MB,前者 Spring Boot 3 默认 1MB)。
- **影响**:N 路并发 × 10MB 直接放大 OOM;且 `application.yml` 未显式设 `max-file-size` / `max-request-size`。
- **建议**:`InputStream` 直传 + 块级 magic-byte 嗅探,显式配 multipart 上限与业务上限对齐。

### A10. `OperationLogController.page` 无 size/时间范围校验 [置信度 85]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/audit/interfaces/OperationLogController.java:34-58` + `frontend/src/views/audit/OperationLogView.vue:163-175`
- **证据**:controller 参数无 `@Min/@Max`;前端提示"时间范围不能超过 31 天",实际无 `value[1] - value[0] <= 31*86400e3` 校验。
- **影响**:已登录用户可 `size=1000000` + `fromEpochMs=0..Long.MAX` 全表拉;前端误导。
- **建议**:controller 补 `@Min(1) @Max(200)` + 时间范围 ≤ 31 天校验;前端补同样判断。

---

## 3. Bug / 回归 / 行为风险(6 条)

### B1. `DatabaseUserRepository` 缓存 `AuthenticationUser` 含 `passwordHash` [置信度 85]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/user/infrastructure/repository/DatabaseUserRepository.java:79-95` + `AuthenticationUser.java`(`password` 字段)
- **证据**:`toAuthenticationUser` 把 `user.getPasswordHash()` 塞进 record;`@Cacheable(value = CacheNames.AUTH_PRINCIPAL)` 命中后 bcrypt/argon2 hash 序列化进 Redis。
- **影响**:登录后该 hash 长期驻留 Redis,备份/AOF 重放/聚合查询 = 离线爆破面。
- **建议**:在缓存前清空 `AuthenticationUser.password`,或将 record 字段改为不写缓存。

### B2. 事务内 `StpUtil.kickout` / `sessionIndexService.removeUser` 不可回滚 [置信度 85]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/auth/application/PasswordResetApplicationService.java:178-185` + `UserManagementService.update/delete`
- **证据**:`@Transactional` 内直接调 Redis 操作,DB 回滚后用户已被踢出。
- **影响**:批量任务/补偿任务中无辜用户被迫下线,无补偿机制。
- **建议**:用 `TransactionSynchronizationManager.registerSynchronization` 在 `afterCommit` 中执行踢出。

### B3. `MailChannel` 删除走硬 DELETE,绕开 `@TableLogic` 与审计 [置信度 80]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/system/application/MailChannelApplicationService.java:130-138` + `SysMailChannelMapper.java:12-14`
- **证据**:`@Delete("DELETE FROM sys_mail_channel WHERE tenant_id = #{tenantId}")` + `hardDeleteByTenantId`;实体有 `@TableLogic` 但未生效。
- **影响**:删除邮件渠道物理删行,SMTP 凭据历史丢失,审计回溯不到原配置人与时间。
- **建议**:改为 `update(... wrapper)` 写 `deleted=1` + 审计 `MAIL_CHANNEL_DELETED`。

### B4. `MailChannelApplicationService.sendTestMail` 把 SMTP 异常 message 透传客户端 [置信度 80]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/system/application/MailChannelApplicationService.java:172-180`
- **证据**:`throw new BusinessException("MAIL_TEST_FAILED", "测试邮件发送失败：" + safeMessage(ex));`
- **影响**:SMTP 错误常含远程主机/端口/AUTH 失败原因,叠加 A2 构成"凭据侦察"链路。
- **建议**:只回固定 code + 通用 message,详细原因仅写 ERROR 日志。

### B5. `MinioObjectStorageService` 异常被吞,无 `log.error` / cause 透传 [置信度 80]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/file/infrastructure/MinioObjectStorageService.java:29-50`
- **证据**:`put/get/delete` 全部 `catch (Exception) { throw new BusinessException(...); }`,无 `log.error`,cause 未传。
- **影响**:`ErrorResponseException` 的 401/403/NoSuchBucket 等细分错误全部并到同一文案,排障靠猜。
- **建议**:catch 内 `log.error(...op bucket key, exception)`,透传 cause,按 `errorCode()` 分桶映射。

### B6. `WorkflowTaskUrgeService.urge` 无频次/冷却/催办人限制 [置信度 80]
- **文件**:`src/main/java/com/enterprise/auth/platform/modules/workflow/application/WorkflowTaskUrgeService.java:55-83, 165-168`
- **证据**:`canUrge` 只校验任务存在,无 Redis 计数、无每日上限、无"仅发起人可催"限制。
- **影响**:脚本可刷海量 `wf_task_urge` 记录;非相关人对他人任务发起催办无任何门槛。
- **建议**:加 Redis 频控(同用户/任务 1min 1 次,每日 5 次),催办人收紧为"流程发起人"。

---

## 4. 缺失测试(合并为 1 条)

### C1. 核心安全 / 工作流 / 代码生成 / 邮件 service 零单测 [置信度 80]
- **文件**:以下服务类在 `src/test/java` 下无对应 `*Test.java`:
  - `PasswordResetApplicationService`(345 行,tokens/重放/频控/枚举防护)
  - `SecurityPolicyApplicationService`(租户/平台策略合并优先级)
  - `LoginAttemptService`(锁定/解锁阈值参数化)
  - `PasswordResetNotificationService`(通道回退/枚举防护/缺通道失败)
  - `WorkflowTaskUrgeService`(频次/权限/催办人)
  - `CodegenResourceRegistrationService`(跨租户异常/事务)
  - `CodegenTemplateService`(builtin 只读/pattern 匹配)
  - `MailChannelSecretService`(缺密钥失败 round-trip)
  - `MailChannelSenderManager`(ssl.trust / startTLS / 超时)
- **影响**:§2 / §3 全部改动无回归保护,任何后续修改都可能静默退化。
- **建议**:至少补 9 个测试类;`pom.xml` 已引 `spring-boot-starter-mail`,可顺手加 GreenMail 跑真实 SMTP。

---

## 5. 观察(非阻断,值得记录)

- **Codegen 模板 fixup**:`V202606050001__fix_codegen_template_default_content.sql` 修正了 `controller-head` 模板缺失的 `{{defaultContent}}` 占位 — 是一个**已修复**的 bug,建议在 CHANGELOG 显式标注。
- **CodeGen 资源注册范围**:`拓展计划.md` §6.10 把"生成后自动注册菜单和权限"列为**未完成范围**,但 diff 中已包含 `CodegenResourceRegistrationService`,实现与计划有偏差(且 A5 / C1 风险真实)。建议同步计划文档或撤回该能力到 P2 后续。
- **`AccountApplicationService.changePassword` 未调 `kickout`**:依据 `拓展计划.md` §4.2.2 "旧会话按版本不一致失效",设计依赖 SaTokenUserContextInterceptor 的 `sessionVersion` 不一致检测 — 当前实现是自增 `session_version` 但未主动踢出。前提是 interceptor 真正做了版本比对,否则 B2 + 此处会双重遗留。下次复测建议跑 `mvn -Dtest=SessionIndexServiceTest` + 手工改密验证旧 token 在 5 分钟内的行为。
- **路径穿越防护**:`LocalObjectStorageService.put/get/delete` 走 `Path.normalize() + startsWith(root)`,符合预期。
- **CSV 注入防护**:`OperationLogCsvExportService.protectFormula` 已处理 `=+-@` 前缀,符合计划 §5.13.1"导出防护"验收点。
- **`MailChannelSecretService` 算法本身无问题**:AES-256-GCM + PBKDF2-65536 + 随机 salt/iv,A2 是密钥缺失降级导致,算法侧 OK。
- **`V202606030003` 字段宽度**:`mail_password` 由 `varchar(255)` 改到 `varchar(1024)`,与 `{enc}v1:` 密文(>120 字节 base64url)兼容,无截断风险。

---

## 6. 审查未覆盖

- `frontend/src/views/*` 中除 `OperationLogView` / `AppNavItem` 外的具体表单(头像裁剪、密码重置流程、流程设计器交互)未完整细读。
- `SysResourceEntity` / `SysRoleResourceEntity` / `TenantContext` 上下文透传机制(影响 C1 中 `CodegenResourceRegistrationService` 评估)未深入基础设施层。
- `AuditService` / `AuditPayloadRedactor` 完整实现未读,`AuditPayloadRedactor.SENSITIVE_KEYWORDS` 覆盖度判断(包含 `apikey` / `privatekey` / `accesskey` / `authheader` / `authorization` / `credential` / `reseturl` / `passwd` / `pwd`)仅为间接推断。
- `SaTokenUserContextInterceptor` 中 `sessionVersion` 不一致如何强制下线(影响 B2 与 `changePassword` 行为)未逐行确认。
- 通知中心 / 邮件 / IM 推送的对接、Redis 频限底层实现(`Bucket4j` / 自研)未确认。
- 前端 `composables/` / `directives/`(如 `v-permission`)未读,无法确认操作按钮的权限控制是否与后端 `@SaCheckPermission` 对齐。
- 整个 diff 没跑 `mvn` / `npm run build` / `mvn test`,构建/类型/lint 由 CI 兜底,本评审不重复。
- 仓库无 `CLAUDE.md`,本评审以 `拓展计划.md` 作为需求基准。

---

## 7. 落地建议(可执行顺序)

按修复成本与影响面排序,建议按以下顺序处理:

1. **A1 / A2 / A4 / A6** — 安全优先级最高,先合紧急修复分支。
2. **A3 / A5 / A7 / A10** — 权限与性能修正,合并到下一个常规迭代。
3. **B1 / B2 / B3 / B4** — 凭据/事务/数据完整性。
4. **B5 / B6 / A8 / A9** — 排障体验与运维安全。
5. **C1** — 测试补齐,与功能迭代并行。
6. **§5 观察项** — 同步更新 `拓展计划.md` / CHANGELOG,作为长期债处理。

---

## 8. 元信息

- **审查者**:本地代理(Agent mode)
- **审查方法**:4 路并行领域评审 + 计划合规过滤
- **置信度门槛**:≥ 80(本文件仅收录)
- **未跑构建/测试**:依赖 CI
- **未审查文件清单**:见 §6