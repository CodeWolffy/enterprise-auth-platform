-- ----------------------------------------------------------------------------
-- V4: 「参数管理」菜单指向新建的 Vben 视图
-- 参数管理(sys_config) 为本项目自建能力，haorong 无对应页面；前端新增
-- views/upms/config/index.vue，故将菜单 component 指向它。
-- 说明：V3 已应用且校验和锁定，菜单微调一律新开迁移，禁止回改 V3。
-- ----------------------------------------------------------------------------
SET time_zone = '+00:00';

UPDATE `sys_menu` SET `component` = 'upms/config/index' WHERE `id` = 350; -- 参数管理
