# 企业认证平台 — 改造项目计划（剩余未完成项）

> 目标：把项目「基座模块」前后端向标准化权限平台看齐；前端整体迁移到 **Vue-Vben-Admin v5**，后端统一规范并保留本项目更强能力。
> 本文件只记录**尚未完成**的改造项与执行细节，作为后续落地的施工清单。
> 更新时间锚点：基于 2026-06 当前进度整理。

---

## 0. 当前环境与运行方式

- **后端**：Spring Boot 3.5 单体，控制器统一前缀 `/api`，端口 `8080`；需 MySQL + Redis + 环境变量 `DB_URL/DB_USERNAME/DB_PASSWORD/REDIS_*`。Flyway 迁移在 `src/main/resources/db/migration/`（已到 `V8`）。
- **新前端**：`frontend-vben/`（Vben v5 monorepo，pnpm）。开发启动：`pnpm -C frontend-vben run dev:ele`，端口 `5777`，dev 代理 `/api → localhost:8080`。
- **旧前端**：`frontend-legacy/`（已归档，手写 Vue3+ElementPlus，端口 5173），不再维护。
- **工具链**：pnpm 10.10.0（经 `npm i -g pnpm` 安装，corepack 在本机 EPERM）；registry 用 npmmirror。

---

## 1. 已完成（供参照，**不再重复**）

- **Phase 2 全部**：Vben 脚手架 + 依赖 + dev 服务；适配层 `request.ts`（`Authorization: Bearer`、`X-Tenant-Id`、按 `ApiResponse.code==='OK'` 解包、错误取 `message`）；`core/auth|user|menu`（登录、`GET /api/auth/me` 同时喂 userInfo/grants/menus、logout）；**滑块验证码登录**（移植旧 `SliderCaptcha` + 重写 `login.vue`）；**动态路由**（`V3__realign_menu_for_vben.sql` 把基座菜单 `component` 指向 Vben 视图 + 转换器生成唯一 route name）。
- **CORS 修复**：dev 代理 `configure` 去除 `Origin/Referer`（后端 `allowed-origins` 仅放行旧前端 5173）。
- **已迁移并验证的页面（约 20 个）**：
  - 基座核心：`role 角色`、`config 参数管理(新建)`、`dept 部门`、`tenant-package 租户套餐`、`user 用户`
  - 基座支撑：`dict 字典`、`dict-value 字典项`、`menu 菜单`、`tenant 租户`、`online-user 在线用户`、`log 操作日志`、`login-log 登录日志`
  - 代码生成：`datasource 数据源`、`gen-table 代码生成`
  - 自建模块：`security 安全策略`、`file 文件管理`、`mail-channel 邮件配置`、`notice 公告管理`、`category 分类配置`、`workflow 工作流`
  - 仪表盘：`dashboard/platform 平台统计`
- **品牌**：app 名称/版权/登录标题 → 「企业认证平台」。
- **菜单 component 对齐**：`V3`（基座菜单）+ `V4`（参数管理）+ `V6`（自建模块/代码生成/工作流）+ `V7`（Dashboard 菜单 100）均已应用；Flyway 历史记录正常。
- **API 全面重指**：所有 `api/` 文件统一指向 `/api/*`，分页/字段映射与后端 DTO 对齐。
- **体验 hardening**：
  - 超管行操作 guard：`role/index.vue` 修正为 `ADMIN`（与后端 `sys_role.role_code` 一致）。
  - 菜单图标映射：`api/core/menu.ts` 增加 Element Plus → iconify 映射表，自动转换 `sys_menu.icon`。
  - 登录页：已隐藏手机号登录入口，仅保留账号密码 + 滑块验证码。
  - 字典系统接入：`utils/dict.ts` 改为调用 `GET /api/system/dicts/values?dictType=`，`store/dict.ts` 缓存结构保持不变。
- **Dashboard 数据接入**：`views/dashboard/platform/index.vue` 接入 `/api/dashboard/stats`，展示用户/租户/角色/文件/存储/登录/在线/趋势/健康度等指标。

---

## 2. 通用迁移「配方」（每个剩余模块照此执行）

> 已在 role/dept/user 等模块验证可行，务必复用。

1. **读后端契约**：`src/main/java/.../modules/<模块>/interfaces/*Controller.java` 及其请求/响应 DTO；角色/部门/租户等列表项常在 `modules/resource/application/CatalogService.java` 的 `*View` record。记录端点路径、查询参数、请求体字段、返回字段。
2. **api 文件重指 `/api/*`**：`getPage(query)` 把 `{current,size,...filters}` 映射为 `{page,size,keyword,...}`；列表读 `response.records/response.total`；**api 函数返回的就是解包后的 data**（不要再 `.data`）。
3. **视图按后端字段适配**：表格列、表单字段改用后端 DTO 字段名；**`v-access:code="'upms:x:action'"` 保持不变**（权限码已一致）；**后端无单条 `getById` 时，编辑用列表行数据回填**（`initForm(row)`）。
4. **扁平列表→树**：后端返回扁平 `List` 时前端构树（见 `api/upms/dept.ts` 的 `buildTree`）。
5. **菜单/角色/租户「菜单树分配」**：菜单树用 `GET /api/menus/grantable-tree`（`MenuTreeNode`：label=`name`、node-key=`id`、children=`children`）；角色/租户已分配菜单后端直接返回 `Set<Long>`（ID 数组）。
6. **状态字段注意**：后端不统一——`dept.enabled`(0/1 int)、`user.enabled`(boolean)、`套餐/菜单.status/del`( '0'/'1' string)。逐模块按 DTO 确认，不要硬套。
7. **编译校验**：对每个改动文件 `curl -s -o /dev/null -w "%{http_code}" http://localhost:5777/src/<相对 src 的路径>`，必须 `200`。
8. **实测**：登录后用 `window.location.hash = '<菜单 path>'` 导航 + 截图，验证渲染与 CRUD。
9. **严禁**改共享文件：`api/request.ts`、`api/core/*`、`store`、`router`、`api/upms/menu.ts` 的 `getTenantMenu/getMenusByRole`（role 依赖）。

---

## 3. Phase 3 — 基座模块页面迁移（**已完成**）

> 所有基座模块页面及对应 API 已迁移完成，菜单 `component` 通过 V3/V4/V6/V7 迁移对齐到 Vben 视图路径。

| 模块 | 状态 | 备注 |
|---|---|---|
| dict 字典 / dict-value 字典项 | ✅ 完成 | 含抽屉表单 |
| menu 菜单 | ✅ 完成 | 树表 + 按钮权限 + 排序 |
| tenant 租户 | ✅ 完成 | 含菜单分配 |
| codegen 代码生成 | ✅ 完成 | 数据源 + 表配置 + 生成下载 |
| online-user 在线用户 | ✅ 完成 | 强制下线 |
| log 操作日志 / login-log 登录日志 | ✅ 完成 | 只读列表 |

---

## 4. 自建/扩展模块（**已完成**）

> 自建模块页面已全部新建，并通过 `V6__fix_menu_components_for_vben_views.sql` 将菜单 `component` 对齐到 Vben 视图路径。

| 模块 | 状态 | 对应视图 |
|---|---|---|
| security 安全策略 | ✅ 完成 | `views/upms/security/index.vue` |
| file 文件管理 | ✅ 完成 | `views/upms/file/index.vue` |
| mail-channel 邮件配置 | ✅ 完成 | `views/upms/mail-channel/index.vue` |
| notice 公告管理 | ✅ 完成 | `views/upms/notice/index.vue` |
| category 分类配置 | ✅ 完成 | `views/upms/category/index.vue` |
| workflow 工作流 | ✅ 完成 | `views/workflow/index.vue`（内部 Tab 切换） |

---

## 5. 跨模块与体验项

### 5.1 字典系统接入（useDict / DictTag → `/api` 字典接口）

- 迁移完成：`utils/dict.ts`（`useDict` hook）改为调用 `GET /api/system/dicts/values?dictType=`；`store/dict.ts`（缓存）保持原结构；`components/dict-tag/index.vue` 已支持 `showClass` 渲染。
- 后续可逐步将各页面「状态/类型」等列从硬编码判断改为 `useDict` + `DictTag`。

### 5.2 运行总览 Dashboard 替换

- 已完成：`views/dashboard/platform/index.vue` 接入 `/api/dashboard/stats`，展示用户/租户/角色/文件/存储/登录/在线/趋势/健康度等指标。
- 菜单 100 通过 `V7__fix_dashboard_menu_route.sql` 指向 `dashboard/platform/index`。

### 5.3 UI / 品牌 / 细节清理

- **超管行操作 guard**：`views/upms/role/index.vue` 的 guard 已修正为 `ADMIN`（与后端 `sys_role.role_code` 一致）。
- **菜单图标**：`api/core/menu.ts` 增加 Element Plus → iconify 映射表（`ELEMENT_ICON_TO_ICONIFY`），自动转换 `sys_menu.icon`。
- **登录页手机号隐藏**：已完成，登录页仅保留账号密码 + 滑块验证码。
- **tenant-package 表单**：`appKey` 已改为字典多选（`useDict('tenant_package_app_key')` + `ElSelect`），提交时自动 Join 为逗号分隔字符串；`descriptionMd` 仍为 textarea。
- **参考项目残留**：已清理删除 `frontend-vben/apps/web-ele/src` 下的 `views/{order,pay,product,promotion}`、`api/{order,pay,product,promotion,home}` 及电商相关组件与仪表盘分析页。

---

## 6. 后端剩余

### 6.1 Phase 1（已**延后**）— 审计/逻辑删除列统一（决策 D1-A）

- 范围：把 `sys_menu / sys_log / sys_login_log / sys_role_menu / sys_tenant_menu` **5 张异类表**统一到主流派 `created_at/created_by/updated_at/updated_by + deleted(tinyint)+@TableLogic`。
- 影响面：~17 个文件，含 `MenuService`+2 个 Facade 的 `delFlag` 引用、`AuditMetaObjectHandler`、新增 Flyway 迁移、**6 个含原生 SQL 的测试类**（`TenantControllerTest/CodegenApplicationServiceP2Test/ResourceAuthorizationControllerTest/DashboardStatsServiceP1Test` 等）。
- 注意：`del_flag` 是既有兼容写法，改成 `deleted` 需要单独评估；当前按用户决定**延后**。若执行：先临时库 dry-run 迁移，再改实体/Service/测试，全程 `mvn test` 守护。

### 6.2 Flyway 迁移管理

- `V3`（菜单对齐）、`V4`（参数菜单）、`V6`（自建模块/代码生成/工作流 component 修复）、`V7`（Dashboard 菜单 100）、`V8`（tenant-package appKey 字典）已存在并应用。
- 后续所有菜单 `component`/schema 改动一律**新开 `V9+`**；**禁止回改已应用的历史迁移**（Flyway 校验和会失败）。
- 整库快照 `src/main/resources/database/enterprise_auth_platform.sql` 应同步 V3/V4/V6/V7/V8 改动（仅文档/手工建库用，不影响 Flyway 运行）。

### 6.3 代码生成模板对齐（Phase 4）

- 模板是 **DB 行**（表 `codegen_template`，列 `content/preview/example`），非 .vm 文件。
- 种子模板已对齐：生成物 = ①后端统一规范（审计/逻辑删除、`ApiResponse`、`@SaCheckPermission("upms:sysmodule:action")`、MyBatis-Plus `ServiceImpl`）+ ②**Vben v5 前端**（`frontend-vben/apps/web-ele/src/api/<module>.ts` 指向 `/api`、`index.vue` 列表、`form.vue` 弹窗、权限码 `upms:sys<module>:action`）。
- 后端 Controller 权限码格式已从 `moduleName:action` 统一为 `upms:sys<module>:action`（如 `upms:sysrole:page`），与项目 `PermissionCodes` 对齐。

---

## 7. Phase 5 — 切换与下线（**已完成**）

1. 构建/路由切到 `frontend-vben/`；旧前端已整体归档为 `frontend-legacy/`。
2. 重建 Playwright e2e 基线（旧前端 e2e 失效，需基于 Vben 新页面重建）。
3. **生产 CORS**：`application-dev.yml` 已更新为 `5777`；生产/ staging 配置保持环境变量注入（`APP_CORS_ALLOWED_ORIGIN` / `STAGING_APP_CORS_ALLOWED_ORIGIN`），不硬编码。后端 CORS 逻辑保持“空列表则不校验”的策略，开发期走 dev 代理 Origin-strip 绕过。
4. 参考项目残留代码已清理删除。

---

## 8. 部署 / 环境注意

- 后端 CORS：`WebMvcConfig` 仅在 `app.cors.allowed-origins` 非空时注册映射；空则不校验。Vben(5777) 走 dev 代理 Origin-strip 绕过；生产必须正式配置来源或同源。
- 登录依赖 `tianai-captcha` 滑块（无 dev 旁路）；自动化 E2E 无法可靠解题，登录链路需人工验证。
- 离线编译：`mvn -o -Dmaven.repo.local=.m2repo ...`（项目内置 `.m2repo`）。

---

## 9. 建议执行顺序与验收

**顺序**：体验 hardening 收尾 → 参考项目残留清理（Phase 5）→ Phase 4 模板 → Phase 5 切换下线。（Phase 1 后端列统一可在任意时间独立排期。）

**每模块验收清单**：
- [ ] api 全部指向 `/api/*`，参数/字段/分页映射正确
- [ ] 视图字段、`v-access` 权限码、（如有）菜单/树分配正确
- [ ] 所有改动文件 `curl .../src/...` = 200
- [ ] 登录后导航到对应菜单，列表/新增/修改/删除（及分配）实测通过
- [ ] 菜单 `component`：复用视图者已 V3 对齐；新建视图者已加 `V5+` 迁移

**整体冒烟**：起 MySQL+Redis+后端（Flyway 应用至最新）→ `pnpm -C frontend-vben run dev:ele` → 登录 → 逐菜单点检无 not-found/红字。

---

## 10. 进度跟踪表

| 模块 / 项 | 类型 | 状态 | 备注 |
|---|---|---|---|
| role 角色 | 基座 | ✅ 完成(实测) | 含菜单分配 |
| config 参数管理 | 基座(新建) | ✅ 完成(实测) | V4 已应用 |
| dept 部门 | 基座 | ✅ 完成(实测) | 扁平→树 |
| tenant-package 套餐 | 基座 | ✅ 完成(编译) | appKey 字典多选 |
| user 用户 | 基座 | ✅ 完成(实测) | roleCode 复选 |
| dict 字典 / dict-value | 基座 | ✅ 完成 | 含字典项子表 |
| menu 菜单 | 基座 | ✅ 完成 | 树表 + 按钮权限 |
| tenant 租户 | 基座 | ✅ 完成 | 含菜单分配 |
| codegen 代码生成 | 基座 | ✅ 完成 | 数据源 + 生成下载 |
| online-user 在线用户 | 支撑 | ✅ 完成 | 强制下线 |
| log 操作日志 | 支撑 | ✅ 完成 | 只读 |
| login-log 登录日志 | 支撑 | ✅ 完成 | 只读 |
| security 安全策略 | 自建 | ✅ 完成 | 单页表单 |
| file 文件管理 | 自建 | ✅ 完成 | 列表 + 上传 |
| mail-channel 邮件配置 | 自建 | ✅ 完成 | 单条配置 |
| notice 公告管理 | 自建 | ✅ 完成 | 列表 + 发布 |
| category 分类配置 | 自建 | ✅ 完成 | 树表 |
| workflow 工作流 | 自建 | ✅ 完成 | 多 Tab 单页 |
| dashboard/platform 平台统计 | 仪表盘 | ✅ 完成 | 接入 /api/dashboard/stats |
| 字典系统接入 useDict/DictTag | 跨模块 | ✅ 完成 | 已改 useDict 调用 /api/system/dicts/values |
| Dashboard 菜单 100 指向 | 体验 | ✅ 完成 | V7 迁移已创建 |
| 超管行操作 guard | 体验 | ✅ 完成 | ROLE_ADMIN -> ADMIN |
| 菜单图标 iconify 映射 | 体验 | ✅ 完成 | api/core/menu.ts 增加映射表 |
| 登录页手机号隐藏 | 体验 | ✅ 完成 | 已隐藏手机号登录入口 |
| 参考项目残留清理 | 体验 | ✅ 完成 | 已删除 views/api 中 order/pay/product/promotion |
| Phase 1 后端列统一 | 后端 | ⏸ 延后 | D1-A |
| Phase 4 代码生成模板 | 后端 | ✅ 完成 | 后端规范 + Vben v5 前端 |
| Phase 5 切换下线 | 收尾 | ✅ 完成 | 旧前端归档 frontend-legacy，CORS 切 5777 |
