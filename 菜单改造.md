# 菜单改造方案

## 1. 改造目标

本次菜单模块改造目标是：**完全对齐 `E:\Myproject\haorong-mall-master` 项目的菜单模型和菜单管理方式**。

核心方向：

- `sys_menu` 表结构向参考项目收敛。
- 菜单只保留菜单运行和授权真正需要的字段。
- 菜单类型统一为：`0=菜单`、`1=按钮`。
- 菜单节点负责路由、图标、组件、排序。
- 按钮节点负责权限标识。
- 租户菜单范围通过 `sys_tenant_menu` 管理。
- 角色菜单权限通过 `sys_role_menu` 管理。
- 前端菜单管理支持配置菜单 `icon`，并支持可视化选择和预览。

## 2. 参考项目菜单模型

参考项目 `haorong-mall-master` 的菜单表为 `sys_menu`，核心字段如下：

| 字段 | 含义 |
|---|---|
| `id` | 主键 |
| `name` | 菜单名称 |
| `permission` | 菜单权限 |
| `path` | 路由 URL |
| `redirect` | 重定向 URL |
| `parent_id` | 父菜单 ID |
| `icon` | 图标 |
| `component` | 页面组件地址 |
| `sort` | 排序 |
| `type` | 类型：`0=菜单`、`1=按钮` |
| `outer_status` | 外链状态：`0=否`、`1=是` |
| `del_flag` | 逻辑删除：`0=正常`、`1=删除` |
| `application_key` | 应用标识 |
| `create_by` | 创建人 |
| `update_by` | 修改人 |
| `create_time` | 创建时间 |
| `update_time` | 修改时间 |

参考项目的运行方式：

- 菜单树通过 `parent_id` 动态构建。
- 当前用户菜单只返回 `type=0` 的菜单节点。
- 权限列表从 `type=1` 的按钮节点读取 `permission`。
- 租户菜单通过 `sys_tenant_menu` 过滤。
- 角色授权通过 `sys_role_menu` 过滤。
- 菜单图标存储在 `icon` 字段，由前端解析和渲染。

## 3. 目标表结构

本项目目标 `sys_menu` 表结构建议如下：

```sql
CREATE TABLE `sys_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `name` varchar(60) NOT NULL COMMENT '菜单名称',
  `permission` varchar(128) DEFAULT NULL COMMENT '菜单权限',
  `path` varchar(255) DEFAULT NULL COMMENT 'URL',
  `redirect` varchar(255) DEFAULT NULL COMMENT '重定向 URL',
  `parent_id` bigint DEFAULT NULL COMMENT '父菜单 ID',
  `icon` varchar(64) DEFAULT NULL COMMENT '图标',
  `component` varchar(255) DEFAULT NULL COMMENT '页面组件地址',
  `sort` int NOT NULL DEFAULT 0 COMMENT '排序',
  `type` char(1) DEFAULT NULL COMMENT '类型：0=菜单；1=按钮',
  `outer_status` char(1) NOT NULL DEFAULT '0' COMMENT '外链状态：0=否；1=是',
  `application_key` varchar(32) DEFAULT NULL COMMENT '应用标识',
  `create_by` varchar(64) DEFAULT NULL COMMENT '创建人',
  `update_by` varchar(64) DEFAULT NULL COMMENT '修改人',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `update_time` datetime DEFAULT NULL COMMENT '修改时间',
  `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '逻辑删除：0=正常；1=删除',
  PRIMARY KEY (`id`),
  KEY `idx_sys_menu_parent` (`parent_id`),
  KEY `idx_sys_menu_type` (`type`),
  KEY `idx_sys_menu_permission` (`permission`),
  KEY `idx_sys_menu_del_flag` (`del_flag`),
  KEY `idx_sys_menu_application_key` (`application_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='菜单表';
```

说明：

- 字段语义完全对齐参考项目。
- 字段命名也建议同步对齐，减少后续维护成本。
- `permission` 长度建议保留 `128`，比参考项目更安全，不影响语义对齐。
- `path`、`redirect`、`component` 建议保留比参考项目更长的长度，避免前端路径或组件路径过长。
- `icon` 建议保留 `64`，因为 `Iconify` 图标名可能超过 `32`，例如 `carbon:application-web`。

## 4. 当前字段迁移关系

| 当前字段 | 目标字段 | 处理方式 |
|---|---|---|
| `id` | `id` | 保留 |
| `menu_name` | `name` | 改名 |
| `grant_key` | `permission` | 改名 |
| `path` | `path` | 保留 |
| `redirect` | `redirect` | 保留 |
| `parent_id` | `parent_id` | 保留 |
| `icon` | `icon` | 保留 |
| `component` | `component` | 保留 |
| `order_no` | `sort` | 改名 |
| `menu_type` | `type` | 改名，保留 `0/1` |
| `outer_status` | `outer_status` | 保留 |
| `application_key` | `application_key` | 保留 |
| `created_by` | `create_by` | 改名 |
| `updated_by` | `update_by` | 改名 |
| `created_at` | `create_time` | 改名 |
| `updated_at` | `update_time` | 改名 |
| `deleted` | `del_flag` | 改名，保留 `0/1` |

## 5. 明确移除字段

以下字段按用户要求完全移除：

| 字段 | 移除原因 | 替代方式 |
|---|---|---|
| `tenant_id` | 参考项目菜单是全局菜单模板 | 租户范围由 `sys_tenant_menu` 控制 |
| `ancestors` | 参考项目不维护祖先链 | 每次基于 `parent_id` 动态构树、动态补祖先 |
| `resource_key` | 参考项目没有资源键 | 菜单稳定识别转为 `id/path/permission/application_key` |
| `route_key` | 参考项目没有路由键 | 前端路由由 `path/component` 驱动 |
| `visible` | 参考项目没有可见字段 | 是否显示由菜单授权、租户分配、菜单类型控制 |
| `enabled` | 参考项目没有启用字段 | 禁用通过删除、取消授权、取消租户分配实现 |
| `is_system` | 参考项目没有系统菜单字段 | 系统保护改由后端权限、初始化数据和删除校验控制 |

## 6. 关联表对齐

本次改造不只对齐 `sys_menu`，还要同步对齐授权链路中的两个关联表：

```text
sys_role_menu
sys_tenant_menu
```

对齐原则：

- 字段命名、字段数量、字段语义与参考项目保持一致。
- 不保留本项目现有的增强审计字段。
- 不在本轮设计里额外引入参考项目没有的业务字段。
- 唯一性、防重复等数据治理能力放到服务层批量保存逻辑处理；如后续确需数据库强约束，再单独评估。

### 6.1 `sys_role_menu`

`sys_role_menu` 是角色授权主链路，表达的是：

```text
某个租户下，某个角色，拥有某个菜单或按钮权限节点。
```

参考项目结构：

| 字段 | 含义 | 说明 |
|---|---|---|
| `id` | 主键 | 角色菜单关联记录 ID |
| `role_id` | 角色 ID | 指向角色表 |
| `menu_id` | 菜单 ID | 指向 `sys_menu.id`，可以是菜单节点，也可以是按钮权限节点 |
| `create_time` | 创建时间 | 授权写入时间 |
| `tenant_id` | 租户 ID | 标识该授权归属哪个租户 |

本项目目标结构：

```sql
CREATE TABLE `sys_role_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `role_id` bigint NOT NULL COMMENT '角色 ID',
  `menu_id` bigint NOT NULL COMMENT '菜单 ID',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `tenant_id` varchar(64) NOT NULL COMMENT '租户 ID',
  PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色菜单关联表' ROW_FORMAT=DYNAMIC;
```

与当前本项目相比，调整点：

| 当前设计 | 目标设计 | 处理方式 |
|---|---|---|
| `created_at` | `create_time` | 改名 |
| `created_by` | 无 | 删除 |
| `tenant_id + role_id + menu_id` 唯一约束 | 无 | 删除，保持参考项目一致 |
| `idx_sys_role_menu_tenant_role` 等增强索引 | 无 | 可删除，保持参考项目一致 |

授权写入规则：

```text
1. 保存角色菜单时，先删除当前 tenant_id + role_id 的旧授权。
2. 再批量插入新的 menu_id 集合。
3. 保存前由服务层去重，避免重复插入。
4. 保存时自动补齐父级菜单，保证菜单树可展示。
5. type=0 的记录用于菜单展示。
6. type=1 的记录用于权限标识解析。
```

权限解析规则：

```text
用户 → 角色 → sys_role_menu → sys_menu
```

```text
读取当前租户下用户角色绑定的 menu_id
→ 关联 sys_menu
→ 过滤 del_flag = '0'
→ 过滤 type = '1'
→ 读取 permission
→ 形成权限集合
```

菜单展示规则：

```text
读取当前租户下用户角色绑定的 menu_id
→ 关联 sys_menu
→ 过滤 del_flag = '0'
→ 过滤 type = '0'
→ 基于 parent_id 构建菜单树
```

### 6.2 `sys_tenant_menu`

`sys_tenant_menu` 是租户菜单范围主链路，表达的是：

```text
某个租户被分配了哪些菜单和按钮权限节点。
```

参考项目结构：

| 字段 | 含义 | 说明 |
|---|---|---|
| `id` | 主键 | 租户菜单关联记录 ID |
| `tenant_id` | 租户 ID | 指向租户 |
| `menu_id` | 菜单 ID | 指向 `sys_menu.id` |
| `create_time` | 创建时间 | 分配写入时间 |
| `create_by` | 创建人 | 分配操作人 |

本项目目标结构：

```sql
CREATE TABLE `sys_tenant_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `tenant_id` varchar(64) NOT NULL COMMENT '租户 ID',
  `menu_id` bigint NOT NULL COMMENT '菜单 ID',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `create_by` varchar(64) DEFAULT NULL COMMENT '创建人',
  PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='租户分配菜单表' ROW_FORMAT=DYNAMIC;
```

与当前本项目相比，调整点：

| 当前设计 | 目标设计 | 处理方式 |
|---|---|---|
| `created_at` | `create_time` | 改名 |
| `created_by` | `create_by` | 改名 |
| `tenant_id + menu_id` 唯一约束 | 无 | 删除，保持参考项目一致 |
| `idx_tenant_menu_tenant`、`idx_tenant_menu_menu` 等增强索引 | 无 | 可删除，保持参考项目一致 |

租户菜单写入规则：

```text
1. 保存租户菜单时，先删除当前 tenant_id 的旧菜单范围。
2. 再批量插入新的 menu_id 集合。
3. 保存前由服务层去重。
4. 保存时自动补齐父级菜单，保证菜单树可展示。
5. 租户范围可以包含 type=0 菜单节点，也可以包含 type=1 按钮权限节点。
```

租户过滤规则：

```text
平台租户：读取全部 sys_menu。
非平台租户：sys_menu 必须同时存在于 sys_tenant_menu 中。
```

### 6.3 三表关系

改造后的菜单权限主链路：

```text
sys_menu
  id
  type
  permission
  parent_id

sys_tenant_menu
  tenant_id
  menu_id → sys_menu.id

sys_role_menu
  tenant_id
  role_id
  menu_id → sys_menu.id
```

运行时有效菜单集合：

```text
当前租户可用菜单集合 = sys_tenant_menu ∩ sys_menu
当前角色已授权菜单集合 = sys_role_menu ∩ sys_menu
当前用户最终菜单集合 = 租户可用菜单集合 ∩ 角色已授权菜单集合
```

平台租户例外：

```text
平台租户不需要经过 sys_tenant_menu 过滤，可以读取全部 sys_menu。
```

最终权限集合：

```text
最终菜单集合中 type = '1' 的节点
→ 读取 permission
→ 作为 Sa-Token 权限码
```

## 7. 菜单语义规则

改造后统一规则：

### 7.1 菜单节点 `type=0`

菜单节点承载：

- `name`
- `path`
- `redirect`
- `parent_id`
- `icon`
- `component`
- `sort`
- `outer_status`
- `application_key`

菜单节点不承载：

- `permission`

也就是：

```text
type = 0 时，permission 应为空。
```

### 7.2 按钮节点 `type=1`

按钮节点承载：

- `name`
- `permission`
- `parent_id`
- `sort`
- `application_key`

按钮节点不承载：

- `path`
- `redirect`
- `icon`
- `component`
- `outer_status`

也就是：

```text
type = 1 时，只作为权限节点，不进入左侧菜单路由。
```

## 8. 后端改造范围

### 8.1 实体字段调整

`SysMenuEntity` 需要改为参考模型：

```text
id
name
permission
path
redirect
parentId
icon
component
sort
type
outerStatus
applicationKey
createBy
updateBy
createTime
updateTime
delFlag
```

移除：

```text
tenantId
ancestors
resourceKey
routeKey
visible
enabled
isSystem
```

### 8.2 请求对象调整

`CreateMenuRequest` 建议改为：

```text
parentId
type
name
permission
path
redirect
icon
component
sort
outerStatus
applicationKey
```

移除：

```text
resourceKey
routeKey
visible
enabled
```

### 8.3 返回对象调整

`MenuTreeNode` 建议改为：

```text
id
type
name
permission
parentId
path
redirect
icon
component
sort
outerStatus
applicationKey
children
```

不再返回：

```text
resourceKey
routeKey
ancestors
visible
enabled
system
```

### 8.4 菜单树构建

当前依赖 `ancestors` 的逻辑全部改为基于 `parent_id` 动态处理。

影响点：

- 运行时菜单树
- 角色授权树
- 租户菜单树
- 删除子节点判断
- 授权自动补父级

新的处理规则：

```text
1. 查询全部未删除菜单。
2. 以 id 建立菜单索引。
3. 通过 parent_id 构树。
4. 授权保存时，基于 parent_id 向上递归补齐父级。
5. 菜单返回时，只返回 type=0。
6. 权限返回时，只读取 type=1 且 permission 不为空的节点。
```

### 8.5 权限解析调整

当前权限解析主要读取 `grant_key`。

改造后改为读取：

```text
permission
```

规则：

```text
角色拥有的 sys_role_menu.menu_id
→ 找到 type=1 的菜单节点
→ 读取 permission
→ 加入权限集合
```

### 8.6 租户菜单过滤调整

当前部分逻辑依赖 `tenant_id = platform` 查询菜单模板。

改造后：

```text
sys_menu 是全局模板，不带 tenant_id。
非平台租户通过 sys_tenant_menu 控制可用菜单范围。
平台租户可读取全部 sys_menu。
```

## 9. 能力包和应用范围改造

当前本项目有能力包资源范围逻辑，部分地方依赖 `resource_key`。

移除 `resource_key` 后，需要改成以下模型之一。

### 推荐模型：能力包绑定菜单 ID

将能力包资源范围从：

```text
capability_code + resource_key + scope_type
```

改成：

```text
capability_code + menu_id + scope_type
```

目标字段：

```text
id
tenant_id 或 package_code
capability_code
menu_id
scope_type
required
create_time
update_time
```

优点：

- 和参考项目的 `sys_tenant_menu.menu_id`、`sys_role_menu.menu_id` 统一。
- 不再需要额外资源键。
- 权限范围、菜单范围都围绕菜单 ID 展开。

风险：

- 菜单 ID 必须稳定。
- 初始化 SQL 和跨环境迁移要统一 ID。

### 备选模型：能力包只绑定 `application_key`

能力包只控制应用级范围：

```text
package_code + application_key
```

租户开通套餐后：

```text
按 application_key 找到所有 sys_menu
批量写入 sys_tenant_menu
```

优点：

- 更贴近参考项目 `application_key` 的使用方式。
- 结构简单。

缺点：

- 无法精细控制到单个菜单或按钮。

### 建议选择

建议采用组合方式：

```text
application_key 控制应用级菜单集合
menu_id 控制精细授权范围
```

也就是：

- 套餐默认按 `application_key` 授权一批菜单。
- 如需精细控制，再用 `menu_id` 白名单/黑名单扩展。

## 10. 前端菜单管理改造

### 10.1 表格字段调整

当前菜单管理表格应改为参考项目字段：

| 列 | 字段 |
|---|---|
| 菜单名称 | `name` |
| 菜单权限 | `permission` |
| 菜单编码 | `component` |
| 菜单路径 | `path` |
| 操作 | 修改 / 删除 / 新增下级 |

移除前端字段：

```text
resourceKey
routeKey
visible
enabled
system
```

### 10.2 表单字段调整

菜单表单目标字段：

```text
上级节点
菜单类型
菜单名称
菜单权限
菜单路径
菜单编码
重定向
图标
外链
排序
应用标识
```

菜单类型为 `0=菜单` 时显示：

```text
菜单名称
菜单路径
菜单编码
重定向
图标
外链
排序
应用标识
```

菜单类型为 `1=按钮` 时显示：

```text
菜单名称
菜单权限
排序
应用标识
```

### 10.3 图标配置能力

参考项目前端支持配置菜单 `icon`。本项目需要从“固定图标池”升级为“可配置图标”。

目标能力：

- 菜单列表按 `icon` 字段渲染图标。
- 新增/编辑菜单时可以选择图标。
- 支持搜索图标。
- 支持预览图标。
- 支持手动输入图标编码。
- 支持参考项目已有图标格式，例如：

```text
carbon:settings
carbon:application-web
carbon:dashboard
carbon:user-avatar
carbon:menu
```

### 10.4 图标技术方案

当前本项目主要使用 `@element-plus/icons-vue`。

如果要完全适配参考项目图标格式，建议引入 `Iconify`：

```bash
npm install @iconify/vue
```

前端渲染策略：

```text
1. 如果 icon 包含 ':'，按 Iconify 图标渲染。
2. 如果 icon 不包含 ':'，尝试按 Element Plus 图标渲染。
3. 如果无法识别，使用默认菜单图标兜底。
```

菜单表格中：

```text
row.icon → IconRenderer → 图标展示
```

菜单表单中：

```text
icon 输入/选择 → 即时预览 → 保存到 sys_menu.icon
```

建议新增通用组件：

```text
frontend/src/components/common/AppIcon.vue
frontend/src/components/common/IconPicker.vue
```

#### `AppIcon.vue` 职责

```text
输入 icon 字符串
输出对应图标
支持 Iconify
支持 Element Plus
支持兜底图标
```

#### `IconPicker.vue` 职责

```text
显示常用图标集合
支持搜索
支持手动输入
支持预览
输出 icon 字符串
```

### 10.5 推荐内置图标集合

先维护一份常用图标集合：

```text
carbon:dashboard
carbon:settings
carbon:user-avatar
carbon:user-role
carbon:tree-view
carbon:menu
carbon:folder
carbon:document
carbon:security
carbon:application-web
carbon:data-table
carbon:cloud-service-management
carbon:notification
carbon:email
carbon:flow
carbon:chart-line
carbon:locked
carbon:unlocked
```

后续可以接入完整图标库搜索。

## 11. 前端路由适配

当前本项目有 `route_key` 和模块清单映射。

移除 `route_key` 后，前端路由策略调整为：

```text
1. 后端返回菜单 path/component/name/icon/sort。
2. 前端根据 component 找到本地页面组件。
3. path 作为路由路径。
4. name 作为页面标题。
5. icon 作为菜单图标。
6. sort 作为排序。
```

需要调整：

- `frontend/src/router/index.ts`
- `frontend/src/router/route-access.ts`
- `frontend/src/app/registry/module-manifest.ts`
- `frontend/src/components/common/AppNav.vue`
- `frontend/src/components/common/AppNavItem.vue`
- `frontend/src/views/platform/MenuManagementView.vue`
- `frontend/src/api/modules/menu.ts`
- `frontend/src/types` 下菜单相关类型

### 11.1 组件映射建议

保留前端组件白名单，避免后端任意字符串动态加载任意模块。

示例：

```text
component = UsersView → frontend/src/views/platform/UsersView.vue
component = MenuManagementView → frontend/src/views/platform/MenuManagementView.vue
component = SystemDictsView → frontend/src/views/system/SystemDictsView.vue
```

也可以支持参考项目路径式组件编码：

```text
component = upms/menu/index
```

通过映射表转换：

```text
upms/menu/index → MenuManagementView
```

## 12. 数据迁移方案

### 12.1 新增迁移脚本

建议新增迁移：

```text
V202606130001__align_sys_menu_to_haorong_model.sql
```

### 12.2 字段改名顺序

建议使用兼容式迁移，避免一次性破坏数据：

```sql
ALTER TABLE `sys_menu` CHANGE COLUMN `menu_name` `name` varchar(60) NOT NULL COMMENT '菜单名称';
ALTER TABLE `sys_menu` CHANGE COLUMN `grant_key` `permission` varchar(128) DEFAULT NULL COMMENT '菜单权限';
ALTER TABLE `sys_menu` CHANGE COLUMN `order_no` `sort` int NOT NULL DEFAULT 0 COMMENT '排序';
ALTER TABLE `sys_menu` CHANGE COLUMN `menu_type` `type` char(1) DEFAULT NULL COMMENT '类型：0=菜单；1=按钮';
ALTER TABLE `sys_menu` CHANGE COLUMN `created_by` `create_by` varchar(64) DEFAULT NULL COMMENT '创建人';
ALTER TABLE `sys_menu` CHANGE COLUMN `updated_by` `update_by` varchar(64) DEFAULT NULL COMMENT '修改人';
ALTER TABLE `sys_menu` CHANGE COLUMN `created_at` `create_time` datetime DEFAULT NULL COMMENT '创建时间';
ALTER TABLE `sys_menu` CHANGE COLUMN `updated_at` `update_time` datetime DEFAULT NULL COMMENT '修改时间';
ALTER TABLE `sys_menu` CHANGE COLUMN `deleted` `del_flag` char(1) NOT NULL DEFAULT '0' COMMENT '逻辑删除：0=正常；1=删除';
```

### 12.3 删除字段顺序

删除前先移除相关索引。

需要删除字段：

```sql
ALTER TABLE `sys_menu`
  DROP COLUMN `tenant_id`,
  DROP COLUMN `ancestors`,
  DROP COLUMN `resource_key`,
  DROP COLUMN `route_key`,
  DROP COLUMN `visible`,
  DROP COLUMN `enabled`,
  DROP COLUMN `is_system`;
```

### 12.4 菜单语义清洗

```sql
UPDATE `sys_menu`
SET `permission` = NULL
WHERE `type` = '0';

UPDATE `sys_menu`
SET `path` = NULL,
    `component` = NULL,
    `redirect` = NULL,
    `icon` = NULL,
    `outer_status` = '0'
WHERE `type` = '1';
```

### 12.5 关联表一致对齐

本轮改造要求 `sys_role_menu` 和 `sys_tenant_menu` 字段、命名、约束与参考项目一致。

#### 12.5.1 `sys_role_menu`

目标字段：

```text
id
role_id
menu_id
create_time
tenant_id
```

字段迁移：

```sql
ALTER TABLE `sys_role_menu`
  CHANGE COLUMN `created_at` `create_time` datetime DEFAULT NULL COMMENT '创建时间';

ALTER TABLE `sys_role_menu`
  DROP COLUMN `created_by`;
```

删除当前本项目增强约束和增强索引：

```sql
ALTER TABLE `sys_role_menu` DROP INDEX `uk_sys_role_menu_tenant_role_menu`;
ALTER TABLE `sys_role_menu` DROP INDEX `idx_sys_role_menu_tenant_role`;
ALTER TABLE `sys_role_menu` DROP INDEX `idx_sys_role_menu_tenant_menu`;
```

如果实际库中的索引名和上述名称不同，需要先从 `information_schema.statistics` 查询后再删除。

最终结构对齐参考项目：

```sql
CREATE TABLE `sys_role_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `role_id` bigint NOT NULL COMMENT '角色 ID',
  `menu_id` bigint NOT NULL COMMENT '菜单 ID',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `tenant_id` varchar(64) NOT NULL COMMENT '租户 ID',
  PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色菜单关联表' ROW_FORMAT=DYNAMIC;
```

#### 12.5.2 `sys_tenant_menu`

目标字段：

```text
id
tenant_id
menu_id
create_time
create_by
```

字段迁移：

```sql
ALTER TABLE `sys_tenant_menu`
  CHANGE COLUMN `created_at` `create_time` datetime DEFAULT NULL COMMENT '创建时间';

ALTER TABLE `sys_tenant_menu`
  CHANGE COLUMN `created_by` `create_by` varchar(64) DEFAULT NULL COMMENT '创建人';
```

删除当前本项目增强约束和增强索引：

```sql
ALTER TABLE `sys_tenant_menu` DROP INDEX `uk_sys_tenant_menu_tenant_menu`;
ALTER TABLE `sys_tenant_menu` DROP INDEX `idx_tenant_menu_tenant`;
ALTER TABLE `sys_tenant_menu` DROP INDEX `idx_tenant_menu_menu`;
```

如果实际库中的索引名和上述名称不同，需要先从 `information_schema.statistics` 查询后再删除。

最终结构对齐参考项目：

```sql
CREATE TABLE `sys_tenant_menu` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `tenant_id` varchar(64) NOT NULL COMMENT '租户 ID',
  `menu_id` bigint NOT NULL COMMENT '菜单 ID',
  `create_time` datetime DEFAULT NULL COMMENT '创建时间',
  `create_by` varchar(64) DEFAULT NULL COMMENT '创建人',
  PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='租户分配菜单表' ROW_FORMAT=DYNAMIC;
```

#### 12.5.3 关联表数据清洗

由于删除数据库唯一约束后，服务层必须先清理重复数据。

`sys_role_menu` 重复数据清理：

```sql
DELETE rm
FROM `sys_role_menu` rm
JOIN `sys_role_menu` keep_rm
  ON keep_rm.`tenant_id` = rm.`tenant_id`
 AND keep_rm.`role_id` = rm.`role_id`
 AND keep_rm.`menu_id` = rm.`menu_id`
 AND keep_rm.`id` < rm.`id`;
```

`sys_tenant_menu` 重复数据清理：

```sql
DELETE tm
FROM `sys_tenant_menu` tm
JOIN `sys_tenant_menu` keep_tm
  ON keep_tm.`tenant_id` = tm.`tenant_id`
 AND keep_tm.`menu_id` = tm.`menu_id`
 AND keep_tm.`id` < tm.`id`;
```

清理无效菜单引用：

```sql
DELETE rm
FROM `sys_role_menu` rm
LEFT JOIN `sys_menu` m ON m.`id` = rm.`menu_id` AND m.`del_flag` = '0'
WHERE m.`id` IS NULL;

DELETE tm
FROM `sys_tenant_menu` tm
LEFT JOIN `sys_menu` m ON m.`id` = tm.`menu_id` AND m.`del_flag` = '0'
WHERE m.`id` IS NULL;
```

## 13. 代码迁移步骤

### 阶段一：数据库模型落地

- 新增菜单字段改名迁移。
- 删除不再使用的字段。
- 清理旧索引。
- 新建目标索引。
- 清理菜单/按钮字段语义。
- 修复 `sys_role_menu` 和 `sys_tenant_menu` 字段命名。
- 删除 `sys_role_menu.created_by`。
- 删除 `sys_role_menu`、`sys_tenant_menu` 当前增强唯一约束和增强索引。
- 将关联表去重、父级补齐、旧授权清理放到服务层保存逻辑中处理。

### 阶段二：后端实体和接口调整

- 修改 `SysMenuEntity`。
- 新增/调整 `SysRoleMenuEntity`，字段严格为 `id/roleId/menuId/createTime/tenantId`。
- 新增/调整 `SysTenantMenuEntity`，字段严格为 `id/tenantId/menuId/createTime/createBy`。
- 修改 `CreateMenuRequest`。
- 修改 `MenuTreeNode`。
- 修改 `MenuService`。
- 修改 `MenuController`。
- 修改角色授权逻辑。
- 修改租户菜单逻辑。
- 修改能力包菜单范围逻辑。
- 删除所有 `resourceKey/routeKey/visible/enabled/isSystem/ancestors/tenantId` 依赖。

### 阶段三：前端 API 类型调整

- 修改 `frontend/src/api/modules/menu.ts`。
- 字段从当前模型切换到参考模型。
- 移除前端 `resourceKey/routeKey/visible/enabled/system`。
- 将 `grantKey` 改为 `permission`。
- 将 `menuName` 改为 `name`。
- 将 `orderNo` 改为 `sort`。
- 将 `menuType` 改为 `type`。

### 阶段四：菜单管理页面调整

- 表格字段切换到参考模型。
- 表单字段切换到参考模型。
- 恢复并优化图标配置能力。
- 图标字段使用 `IconPicker`。
- 菜单列表使用 `AppIcon` 渲染图标。
- 操作列保留：修改菜单、删除菜单、新增下级。
- 可选恢复：批量生成按钮权限。

### 阶段五：动态路由适配

- 前端运行时菜单从 `path/component/name/icon/sort` 生成路由。
- 移除 `route_key` 依赖。
- 保留组件白名单。
- 适配参考项目组件编码格式。

### 阶段六：测试和回归

需要覆盖：

- 菜单树查询。
- 新增菜单。
- 修改菜单。
- 删除菜单。
- 新增按钮权限。
- 角色授权菜单。
- 租户分配菜单。
- 登录后菜单树。
- 登录后权限集合。
- 图标选择和回显。
- 动态路由加载。

## 14. 主要影响范围

### 后端

```text
src/main/java/com/enterprise/auth/platform/modules/menu
src/main/java/com/enterprise/auth/platform/modules/role
src/main/java/com/enterprise/auth/platform/modules/tenant
src/main/java/com/enterprise/auth/platform/modules/auth
src/main/java/com/enterprise/auth/platform/common/authz
src/main/resources/db/migration
```

### 前端

```text
frontend/src/api/modules/menu.ts
frontend/src/views/platform/MenuManagementView.vue
frontend/src/app/registry/module-manifest.ts
frontend/src/router/index.ts
frontend/src/router/route-access.ts
frontend/src/components/common/AppNav.vue
frontend/src/components/common/AppNavItem.vue
frontend/src/components/common/AppIcon.vue
frontend/src/components/common/IconPicker.vue
```

### 测试

```text
src/test/java/com/enterprise/auth/platform/resource
src/test/java/com/enterprise/auth/platform/role
src/test/java/com/enterprise/auth/platform/tenant
src/test/java/com/enterprise/auth/platform/auth
frontend/e2e
```

## 15. 风险点

### 15.1 删除 `resource_key` 后的迁移定位问题

原来大量 SQL 使用 `resource_key` 定位菜单。

改造后必须改为：

```text
固定 id
或 path
或 permission
或 name + parent_id
```

建议：菜单初始化数据统一使用固定 ID。

### 15.2 删除 `route_key` 后的前端路由映射问题

前端不能再通过 `route_key` 找模块清单。

需要改为：

```text
component → 本地组件映射
path → 路由路径
name → 标题
icon → 图标
```

### 15.3 删除 `visible/enabled` 后的隐藏/禁用能力下降

如果未来仍需要“隐藏但保留授权”或“禁用但不删除”，需要重新引入状态字段。

完全对齐参考项目后，这些能力默认不保留。

### 15.4 删除 `is_system` 后的系统菜单保护下降

原本系统菜单不能删除。

改造后建议用以下方式保护：

- 后端校验固定 ID 范围。
- 初始化菜单不开放删除。
- 或仅超级管理员可删除。

### 15.5 图标库引入体积

引入 `Iconify` 后要控制图标加载方式。

建议：

- 优先使用按需图标。
- 常用图标本地列表化。
- 不要一次性打包整套图标库。

### 15.6 关联表删除唯一约束后的重复数据风险

完全对齐参考项目后，`sys_role_menu` 和 `sys_tenant_menu` 不再依赖数据库唯一约束防重。

风险：

```text
同一租户 + 同一角色 + 同一菜单 可能重复写入 sys_role_menu
同一租户 + 同一菜单 可能重复写入 sys_tenant_menu
```

处理方式：

```text
1. 保存前服务层对 menu_id 去重。
2. 保存角色授权时先删除 tenant_id + role_id 的旧数据，再批量插入新数据。
3. 保存租户菜单时先删除 tenant_id 的旧数据，再批量插入新数据。
4. 查询权限和菜单时使用 Set 或 distinct 去重兜底。
5. 迁移前先清理历史重复数据。
```

### 15.7 `sys_role_menu` 删除 `created_by` 后的审计能力下降

完全对齐参考项目后，`sys_role_menu` 只保留 `create_time`，不再记录授权创建人。

如果后续需要追踪“谁给角色分配了菜单”，建议放到操作日志或审计日志中，不再放在 `sys_role_menu` 表内。

## 16. 推荐落地顺序

建议按以下顺序实施：

```text
1. 新增 AppIcon / IconPicker，先把菜单 icon 配置能力做好。
2. 修改前端菜单管理，让页面先适配目标字段结构的中间模型。
3. 新增数据库迁移，完成 sys_menu 字段改名和字段删除。
4. 同步迁移 sys_role_menu、sys_tenant_menu，使字段、命名、约束与参考项目一致。
5. 修改后端实体和接口字段。
6. 修改菜单树、权限解析、角色授权、租户菜单逻辑。
7. 修改能力包和套餐绑定逻辑。
8. 修改动态路由逻辑，移除 route_key。
9. 清理旧 SQL 和旧测试。
10. 跑后端测试、前端 lint、E2E 菜单流。
```

## 17. 最终验收标准

改造完成后，应满足：

- `sys_menu` 字段和参考项目语义一致。
- `sys_role_menu` 字段与参考项目一致，只保留 `id/role_id/menu_id/create_time/tenant_id`。
- `sys_tenant_menu` 字段与参考项目一致，只保留 `id/tenant_id/menu_id/create_time/create_by`。
- `sys_role_menu` 和 `sys_tenant_menu` 不再保留本项目增强唯一约束和增强索引。
- 不再依赖 `tenant_id/ancestors/resource_key/route_key/visible/enabled/is_system`。
- 菜单树只通过 `parent_id` 构建。
- 登录菜单只返回 `type=0`。
- 权限集合只读取 `type=1` 的 `permission`。
- 租户菜单只通过 `sys_tenant_menu` 控制。
- 角色授权只通过 `sys_role_menu` 控制。
- 前端菜单管理可以配置、预览、保存菜单 `icon`。
- 前端菜单列表和左侧导航都能正确渲染 `icon`。
- 动态路由不再依赖 `route_key`。
- 所有菜单相关测试通过。

## 18. 结论

本次改造可行，但属于菜单权限主链路重构。

建议不要只做数据库字段删除，而是按以下目标完整推进：

```text
数据库字段对齐参考项目
后端菜单/权限/租户链路同步重构
前端菜单管理和动态图标能力同步升级
动态路由从 route_key 切换到 path/component
```

这样才能真正完成“完全对齐参考项目”，而不是只在表结构上表面一致。

落地时需要同时完成四条主线：

```text
1. 数据库结构对齐
   - sys_menu 字段收敛到目标结构。
   - sys_role_menu 字段收敛到参考项目结构。
   - sys_tenant_menu 字段收敛到参考项目结构。
   - 删除本项目额外唯一约束和增强索引。

2. 后端菜单链路对齐
   - 菜单树只依赖 parent_id。
   - 登录菜单只返回 type=0。
   - 权限集合只读取 type=1 的 permission。
   - 租户范围只通过 sys_tenant_menu 控制。
   - 角色授权只通过 sys_role_menu 控制。

3. 前端菜单管理对齐
   - 菜单维护页支持 icon 配置、选择、预览和保存。
   - 菜单列表支持 icon 展示。
   - 左侧导航支持 icon 渲染。
   - 动态路由使用 path/component/name/icon，不再依赖 route_key。

4. 历史数据和兼容风险处理
   - 迁移前清理重复授权数据。
   - 初始化菜单使用稳定 ID 或稳定业务键。
   - 删除 visible/enabled/is_system 后，用服务层规则补足必要保护。
   - 对角色授权、租户授权、登录菜单、按钮权限做回归验证。
```

本方案不建议拆成“只改字段”的小改动。

原因是菜单字段、租户菜单范围、角色授权关系、登录菜单树、前端动态路由和图标渲染是同一条主链路。只删除数据库字段会导致后端实体、查询条件、前端路由、菜单管理表单和权限解析同时出现断点。

最终推荐按“先前端兼容目标模型，再迁移数据库，再切后端逻辑，最后删除旧字段依赖”的方式推进。

这样可以降低一次性切换风险，也方便在迁移过程中保持菜单可见、权限可用、路由可进、图标可配。

## 19. 实施检查清单

### 19.1 数据库

- [ ] `sys_menu` 已新增或确认存在 `icon`。
- [ ] `sys_menu` 已新增或确认存在 `redirect`。
- [ ] `sys_menu` 已新增或确认存在 `outer_status`。
- [ ] `sys_menu` 已新增或确认存在 `application_key`。
- [ ] `sys_menu.menu_name` 已迁移为 `name`。
- [ ] `sys_menu.menu_type` 已迁移为 `type`。
- [ ] `sys_menu.order_num` 已迁移为 `sort`。
- [ ] `sys_menu.perms` 已迁移为 `permission`。
- [ ] `sys_menu` 已移除 `tenant_id`。
- [ ] `sys_menu` 已移除 `ancestors`。
- [ ] `sys_menu` 已移除 `resource_key`。
- [ ] `sys_menu` 已移除 `route_key`。
- [ ] `sys_menu` 已移除 `visible`。
- [ ] `sys_menu` 已移除 `enabled`。
- [ ] `sys_menu` 已移除 `is_system`。
- [ ] `sys_role_menu` 已收敛为 `id/role_id/menu_id/create_time/tenant_id`。
- [ ] `sys_tenant_menu` 已收敛为 `id/tenant_id/menu_id/create_time/create_by`。
- [ ] 历史重复授权数据已清理。
- [ ] 菜单初始化 SQL 已改为目标字段结构。

### 19.2 后端

- [ ] 菜单实体字段已对齐目标表结构。
- [ ] 菜单新增、编辑、查询接口已对齐目标字段。
- [ ] 菜单树构建已移除 `ancestors` 依赖。
- [ ] 登录菜单查询已过滤 `type=0`。
- [ ] 按钮权限查询已过滤 `type=1`。
- [ ] 权限解析已使用 `permission`。
- [ ] 动态路由数据已使用 `path/component/name/icon`。
- [ ] 角色授权保存前已对 `menu_id` 去重。
- [ ] 租户菜单保存前已对 `menu_id` 去重。
- [ ] 授权查询结果已使用去重兜底。
- [ ] 系统菜单保护已迁移到服务层规则或操作权限规则。

### 19.3 前端

- [ ] 菜单管理表单已支持 `icon`。
- [ ] 菜单管理表单已支持 `redirect`。
- [ ] 菜单管理表单已支持 `outer_status`。
- [ ] 菜单管理表单已支持 `application_key`。
- [ ] 菜单管理表单已移除旧字段依赖。
- [ ] 图标选择器已接入。
- [ ] 菜单列表已展示图标。
- [ ] 左侧导航已渲染图标。
- [ ] 动态路由已移除 `route_key` 依赖。
- [ ] 路由组件映射已改为基于 `component`。
- [ ] 菜单标题已使用 `name`。
- [ ] 按钮权限已使用 `permission`。

### 19.4 回归验证

- [ ] 超级管理员登录菜单正常。
- [ ] 租户管理员登录菜单正常。
- [ ] 普通角色登录菜单正常。
- [ ] 未授权菜单不可见。
- [ ] 已授权菜单可见。
- [ ] 未授权按钮不可用。
- [ ] 已授权按钮可用。
- [ ] 菜单新增后可在左侧导航显示。
- [ ] 菜单编辑后图标、标题、路径同步生效。
- [ ] 菜单删除后角色授权和租户授权不再产生脏引用。
- [ ] 能力包绑定菜单正常。
- [ ] 套餐绑定能力包后租户菜单范围正常。
- [ ] 前端刷新后动态路由仍可恢复。
- [ ] 后端菜单相关测试通过。
- [ ] 前端 lint 通过。
- [ ] 菜单核心 E2E 流程通过。

## 20. 最小可交付范围

如果需要分阶段交付，建议第一阶段至少完成：

```text
1. sys_menu 字段迁移。
2. sys_role_menu / sys_tenant_menu 字段迁移。
3. 后端菜单查询、登录菜单、权限解析改造。
4. 前端菜单管理 icon 配置。
5. 前端动态路由移除 route_key。
6. 菜单初始化 SQL 更新。
7. 登录、菜单展示、角色授权、租户授权四条主流程验证。
```

低于这个范围，不建议上线。

因为只完成其中一部分，会造成菜单数据能保存但不能渲染、权限能分配但不能解析、路由能生成但页面进不去等链路断裂问题。

## 21. 上线前确认口径

上线前需要确认以下决策：

```text
1. 是否完全删除 visible/enabled/is_system。
2. 是否接受 sys_role_menu/sys_tenant_menu 不再使用唯一约束防重。
3. 是否保留 application_key。
4. 是否所有菜单初始化数据都使用稳定 ID。
5. 是否允许历史菜单 ID 变化。
6. 是否需要兼容旧版前端一段时间。
7. 是否需要给旧权限标识做兼容映射。
8. 是否需要回滚 SQL。
```

这些决策确认后，再进入实际开发和迁移。